---
marp: true
theme: default
paginate: true
header: "第8回: メモリ回路と非同期読み出し"
footer: "© 2026 Hideaki YANAGISAWA, Dept. of Computer Science and Electronic Engineering, NIT, Tokuyama College"
author: "Hideaki YANAGISAWA"
style: |
  section {
    font-family: 'Helvetica Neue', Arial, 'Hiragino Kaku Gothic ProN', sans-serif;
    font-size: 22px;
    padding: 32px 40px;
  }
  h1 { color: #1e3a8a; font-size: 34px; }
  h2 { color: #1e40af; border-bottom: 2px solid #3b82f6; font-size: 26px; }
  pre { font-size: 16px; margin: 8px 0; }
  .box { background: #eff6ff; border-left: 5px solid #3b82f6; padding: 8px 14px; margin: 8px 0; }
  .badge { background: #dbeafe; color: #1e40af; padding: 2px 6px; border-radius: 4px; font-weight: bold; font-size: 16px; }
---

# 第8回: メモリ回路と非同期読み出し
### 〜メモリ階層・レジスタファイルとRAWフォワーディング回路〜

<br>

- **資料作成者**: Hideaki YANAGISAWA
- **講義時間**: 60分
- **対象**: デジタル論理回路基礎習得者
- **本日のアジェンダ**:
  1. デジタルシステムにおける記憶素子の階層構造
  2. レジスタ配列 vs SRAMブロック
  3. Chiselのメモリプリミティブ: `Mem` と `SyncReadMem`
  4. **非同期読み出し (Asynchronous Read)** の動作原理とタイミング
  5. クロック同期書き込み (Synchronous Write) と書き込みイネーブル
  6. 2R1W レジスタファイル (Register File) のアーキテクチャ
  7. 発展トピック: Read-After-Write (RAW) 競合とフォワーディング回路
  8. 全8回の総括と高度なシステム設計へのロードマップ

<!-- Note:
いよいよ本講座の最終回、第8回講義へようこそ。
本講義では、プロセッサやデータ処理回路に大量のデータを蓄積するための「メモリ回路（Memory Circuits）」を扱います。
これまで学んだ D-FF（Reg）と、ハードウェアの「メモリブロック（SRAM / Register File）」はどう違うのか？
Chiselが提供する Mem（非同期読み出し）と SyncReadMem（同期読み出し）の物理的な挙動の違いを深く掘り下げ、RISC-V等のCPUコアの基幹部品であるレジスタファイルを完成させます。
-->

---

## 1. デジタルハードウェアにおける記憶素子の階層

ハードウェアの記憶素子は、「アクセス速度」と「回路面積（ビット単価）」のトレードオフによって階層化されています。

```
 [ 最速 / 最大面積 ]  個別レジスタ (D-FF: RegInit)  ─── 制御フラグ、状態レジスタ
        ▲
        │          レジスタファイル (Register File) ─── CPUレジスタ群 (Mem: 非同期読出)
        │
        ▼          ブロックRAM (BRAM / SRAM)       ─── キャッシュ、バッファ (SyncReadMem: 同期読出)
 [ 低速 / 最小面積 ]  外部DRAM (DDR4/5)               ─── メインメモリ (コントローラ経由)
```

- **フリップフロップ群 (`RegInit`)**: 全ビットに独立配線。面積は巨大だが1サイクルで自在に並列アクセス可能。
- **メモリ構造 (`Mem`)**: アドレスデコーダを共有し、指定した1ワードのみを読み書きする高密度構造。

<!-- Note:
記憶素子のピラミッド構造です。
全部のメモリをRegで作れば最速ですが、1ビットあたり十数個のトランジスタを消費するためチップ面積が爆発します。
一方、SRAMやメモリブロックはアドレスデコーダを共通化してビットセルを格子状に並べるため、面積を劇的に小さくできます。
その代わり、1回にアクセスできるアドレスが制限されます。
-->

---

## 2. 非同期読み出し vs 同期読み出し

メモリの読み出し方式には、工学的に極めて重要な2つの方式があります。

<div style="display: flex; justify-content: space-between;">
<div style="width: 48%;">

### 非同期読み出し (Combinational Read)
アドレス `addr` を与えると、**クロックを待たずに同一サイクル内で組合せ回路として即座にデータが出力**される。

```
raddr ───> [ 組合せデコード ] ───> rdata
         (無遅延・同一サイクル)
```

- **Chisel型**: `Mem(size, type)`
- **用途**: CPUの汎用レジスタファイル。

</div>
<div style="width: 48%;">

### 同期読み出し (Synchronous Read)
アドレスを与えた後、**次のクロックの立ち上がりエッジ**でデータが確定して出力される（1サイクル遅延）。

```
raddr ───> [ クロックサンプリング ] ──> rdata
         (1クロック後に出力確定)
```

- **Chisel型**: `SyncReadMem(size, type)`
- **用途**: FPGAのBRAM、大容量キャッシュ。

</div>
</div>

<!-- Note:
本日の核心テーマである「非同期読み出し vs 同期読み出し」です。
非同期読み出しは、アドレスを与えた瞬間にデータが出てきます。つまり「巨大なマルチプレクサ」として振る舞います。
一方、同期読み出しはアドレスを与えてクロックが1回カチッと鳴って初めてデータが出てきます。
FPGAの大容量BRAMは同期読み出ししかできませんが、CPUのレジスタファイルのように「同じサイクルで読んだ値を使って即座にALUで計算したい」場合は、非同期読み出しメモリ（分散RAMやFFアレイ）が不可欠になります。
-->

---

## 3. Chiselにおける `Mem`: 非同期読み出しメモリ

Chiselで非同期読み出しメモリを生成するには、`Mem` プリミティブを使用します。

```scala
// 8ワード、各ワード8ビット幅のメモリ配列を生成
val mem = Mem(8, UInt(8.W))

// 1. 同期書き込み (クロック立ち上がりで書き込み)
when(io.wen) {
  mem(io.waddr) := io.wdata
}

// 2. 非同期読み出し (アドレス指定で即座にデータが出力)
io.rdata := mem(io.raddr)
```

- **書き込み**: 常にクロックに同期（`when(wen)` で指定）。
- **読み出し**: 純粋な組合せ論理として動作（クロック遅延なし）。

<!-- Note:
Chiselでの Mem の書き方です。
非常に直感的です。
mem(io.waddr) := io.wdata を when(io.wen) の中に書くことで、書き込みイネーブル付きの同期書き込みになります。
そして io.rdata := mem(io.raddr) と書くだけで、クロックを待たずに即座にデータが読み出される非同期ポートが生成されます。
-->

---

## 4. マルチポート・レジスタファイル (Register File)

現代のプロセッサ（RISC-V等）では、1命令で2つのレジスタから値を読み出し、1つのレジスタへ書き戻すため、**「2読み出しポート・1書き込みポート (2R1W)」**の構成が標準です。

```
                  ┌───────────────────────────────┐
 io.wdata (8bit) ─┤                               ├─> io.rdata1 (8bit)
 io.waddr (3bit) ─┤           Register            │   (raddr1 に対応)
 io.wen   (1bit) ─┤          File (8x8)           │
                  │                               ├─> io.rdata2 (8bit)
 io.raddr1(3bit) ─┤                               │   (raddr2 に対応)
 io.raddr2(3bit) ─┤                               │
                  └───────────────────────────────┘
```

- 2つの読み出しポートは互いに完全に独立しており、同一サイクルに異なる2つのアドレスを同時に読み出せる。

<!-- Note:
2R1Wレジスタファイルの構造です。
例えば加算命令 `add r3, r1, r2` を実行するとき、プロセッサは1サイクルの間に r1 と r2 の両方を同時に読み出し、ALUに渡す必要があります。
したがって、読み出しポートが独立して2本必要になります。
Chiselではこれを驚くほどシンプルにモデル化できます。
-->

---

## 5. 2R1W レジスタファイルの実装コード

```scala
class RegisterFile8x8 extends Module {
  val io = IO(new Bundle {
    // 読み出しポート1 & 2 (非同期)
    val raddr1 = Input(UInt(3.W))
    val rdata1 = Output(UInt(8.W))
    val raddr2 = Input(UInt(3.W))
    val rdata2 = Output(UInt(8.W))

    // 書き込みポート (同期)
    val wen    = Input(Bool())
    val waddr  = Input(UInt(3.W))
    val wdata  = Input(UInt(8.W))
  })

  val mem = Mem(8, UInt(8.W))

  // 同期書き込み
  when(io.wen) {
    mem(io.waddr) := io.wdata
  }

  // 2つの独立した非同期読み出しポート
  io.rdata1 := mem(io.raddr1)
  io.rdata2 := mem(io.raddr2)
}
```

<!-- Note:
本日の基本問題の完全なコードです。
同じ mem インスタンスに対して、異なるアドレス raddr1 と raddr2 でインデックス参照するだけで、ハードウェアとして2つの独立した読み出しデコード回路が並列に合成されます。
これがChiselの高水準な記述力です。
-->

---

## 6. 【最重要工学的課題】Read-After-Write (RAW) 競合

### RAW競合とは
「**同一クロックサイクル**において、**同一アドレス**に対して書き込みと読み出しが同時に発生した場合、読み出しポートには何が出力されるべきか？」

$$waddr == raddr1 \quad \text{かつ} \quad wen == 1$$

### 物理メモリの未定義動作
- 書き込みデータがメモリセルに定着するのは「次のクロックの立ち上がりエッジ」。
- 一方、非同期読み出しは「現在のサイクル中」にセルを覗き見る。
- $\implies$ **古い値（更新前のデータ）が読まれるか、過渡的な不定値 (X) が読まれてしまう！**

<!-- Note:
ハードウェア設計の超重要トピック「RAW競合」です。
命令パイプラインで「r1に10を書き込む」命令の直後に「r1の値を読む」命令が来たとき、メモリセルへの書き込みが完了する前に読み出しが行われると、古いデータが読まれて大惨事になります。
これに対処するのが、プロセッサの高速化の要である「バイパス（フォワーディング）回路」です。
-->

---

## 7. RAWフォワーディング（バイパス）回路の原理

メモリセルへの書き込みを待たずに、**書き込みデータを直接読み出しポートへショートカット（迂回）**させる。

```
 io.wdata ───────────┬─────────────────────────────────┐
                     │ (書き込みデータ)                │
                     ▼                                 ▼
               [ Mem セル配列 ] ──> [ 古いデータ ] ─> [ MUX ] ──> io.rdata1
                     ▲                                  ▲
 io.wen, waddr ──────┴──> [ 一致判定比較器 ] ───────────┘
                           (waddr == raddr1 && wen)
```

- **バイパス判定条件**:
  $$bypass = wen \land (waddr == raddr)$$
- $bypass$ が真ならば、メモリセルを通さず `wdata` を即座に出力！

<!-- Note:
フォワーディング回路のブロック図です。
発想は極めてシンプルかつ強烈です。
「同じアドレスに今まさに書こうとしているなら、メモリセルを通さずに、入力ピンにある最新の wdata をそのまま読み出しピンにバイパス（直結）してしまえば良い」というアプローチです。
これにより、同一サイクル内での最新データの可視性が100%保証されます。
-->

---

## 8. RAWバイパス回路付きレジスタファイルの実装

```scala
class BypassedRegisterFile extends Module {
  val io = IO(new Bundle {
    val raddr1 = Input(UInt(3.W)); val rdata1 = Output(UInt(8.W))
    val raddr2 = Input(UInt(3.W)); val rdata2 = Output(UInt(8.W))
    val wen    = Input(Bool());    val waddr  = Input(UInt(3.W))
    val wdata  = Input(UInt(8.W))
  })

  val rf = Module(new RegisterFile8x8)
  rf.io.raddr1 := io.raddr1; rf.io.raddr2 := io.raddr2
  rf.io.wen    := io.wen;    rf.io.waddr  := io.waddr; rf.io.wdata := io.wdata

  // RAW競合の検出
  val bypass1 = io.wen && (io.waddr === io.raddr1)
  val bypass2 = io.wen && (io.waddr === io.raddr2)

  // MUXによるフォワーディング
  io.rdata1 := Mux(bypass1, io.wdata, rf.io.rdata1)
  io.rdata2 := Mux(bypass2, io.wdata, rf.io.rdata2)
}
```

<!-- Note:
発展問題の模範解答コードです。
先ほど作った RegisterFile8x8 をサブモジュールとして内包し、その外側に bypass 判定と比較器、そして Mux を配置しています。
これぞまさに第5回で学んだ構造化設計の真骨頂です。
-->

---

## 9. タイミング波形によるRAWフォワーディングの検証

アドレス `2` に対して、書き込み (`0x42`) と読み出しが同一サイクルで衝突した場合：

```
CLK      : ──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐
             └──┘  └──┘  └──┘  └──┘  └──┘  └──
waddr    : ──X         2        X─────────────
wdata    : ──X        0x42      X─────────────
wen      : ──|‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾|─────────────
raddr1   : ──X         2        X─────────────
bypass1  : ──|‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾|───────────── (即座に検出)
rdata1   : ──X        0x42      X───────────── (クロックエッジを待たずに0x42が出現！)
```

- クロックエッジの到着を待たずに、同じサイクル内で即座に `0x42` が出力される。
- これにより、後続のALUステージがストール（待ち時間）なしで演算を継続できる。

<!-- Note:
タイミング波形です。
クロックエッジが来る前のサイクルの中で、すでに rdata1 に最新データ 0x42 が現れていることが確認できます。
もしバイパス回路がないと、このサイクルでは古いデータ（あるいは不定値）が出てしまい、CPUパイプラインを1サイクル停止（ストール）させなければならなくなります。
このわずかな回路が、プロセッサのIPC（1サイクルあたり実行命令数）を飛躍的に向上させます。
-->

---

## 10. `SyncReadMem`（同期読み出し）との使い分け指針

実務において `Mem` と `SyncReadMem` をどう選択すべきか？

| 特性 | `Mem` (非同期読み出し) | `SyncReadMem` (同期読み出し) |
| :--- | :--- | :--- |
| **読み出しレイテンシ** | **0サイクル (組合せ回路)** | **1サイクル (同期レジスタ)** |
| **物理ハードウェア** | 分散RAM (LUT-RAM) / FF配列 | **専用ブロックRAM (BRAM)** |
| **集積規模** | 小容量向け (数KB以下) | **大容量向け (数十KB〜数MB)** |
| **最大動作周波数** | 低〜中 (組合せ遅延大) | **極めて高い (専用高速マクロ)** |
| **代表的な用途** | **CPUレジスタファイル**, 小規模FIFO | **L1/L2キャッシュ**, パケットバッファ |

<!-- Note:
実務設計におけるメモリ選定の極意です。
初心者は全部非同期の Mem で作りたがりますが、FPGAやASICでは大容量のメモリをLUTやFFで作るとチップ面積をあっという間に使い果たしてしまいます。
したがって、小規模なレジスタファイルは Mem、大規模なデータバッファは SyncReadMem、というように目的と容量に応じて厳格に使い分けるのがプロの技法です。
-->

---

## 11. 全8回 講義シリーズの総括（マイルストーン達成）

本講座を通じて皆さんが習得されたハードウェア設計能力：

1. **組合せ回路**: ブール代数、DAG、ビット幅安全な全加算器・加減算器
2. **MUX・デコーダ**: 条件代入と**透過ラッチの完全撲滅 (`WireDefault`)**
3. **同期順序回路**: D-FF、暗黙クロック・同期リセット、任意進数カウンタ
4. **特殊カウンタ**: Ring/Johnsonカウンタと**自己復帰保護論理**、グレイコード
5. **構造化設計**: サブモジュール階層化とポートバインド、階層型ALU
6. **複合システム**: **非同期リップルクロックの排除**と60進/デジタル時計
7. **制御論理**: **グリッチフリーなMoore型FSM**と `ChiselEnum`
8. **記憶システム**: 非同期読み出しメモリと**RAWフォワーディング回路**

<!-- Note:
全8回の振り返りです。
第1回で1ビットのゲートからスタートした皆さんは、加算器、デコーダ、カウンタ、ALU、時計、FSM、そしてメモリとRAWフォワーディングまで、現代のコンピュータアーキテクチャを構成するすべての基本ビルディングブロックを、型安全なChisel言語で自らの手で設計できるようになりました。
-->

---

## 12. デジタルシステム設計者としての次のステップ

本講座を修了した皆さんの前に広がる、より高度なハードウェアの世界：

```
 ┌────────────────────────────────────────────────────────┐
 │            本講座修了：ハードウェア基礎の完全習得      │
 └───────────────────────────┬────────────────────────────┘
                             │
         ┌───────────────────┴───────────────────┐
         ▼                                       ▼
 ┌─────────────────────────┐           ┌─────────────────────────┐
 │ CPUプロセッサコアの設計 │           │ 高速インターフェース設計│
 │ - 単一サイクル RISC-V   │           │ - Ready/Valid ハンドシェイク
 │ - 5段パイプライン化     │           │ - 非同期FIFO / CDC設計  │
 │ - キャッシュメモリ制御  │           │ - AXI4 / TileLink バス │
 └─────────────────────────┘           └─────────────────────────┘
```

- 本講座で学んだ全モジュールが、そのままRISC-V CPUの構成要素になります！

<!-- Note:
今後の展望です。
皆さんが作ったALU、レジスタファイル、FSM、カウンタを1つのトップモジュールにまとめると、なんと世界標準のオープン命令セットアーキテクチャである「RISC-V CPUコア」が完成します。
本講座で培った「完全同期設計」「ラッチ防止」「Moore型FSM」「RAWフォワーディング」の原則は、最先端のプロセッサ設計でもそのまま100%通用する普遍的な知識です。
-->

---

## 13. 実機実装（FPGA / ASIC）への橋渡し

Chiselで記述したコードがどのようにシリコンチップやFPGAになるのか：

```
 [ Chisel (Scala) ソースコード ]
              │
              ▼ (Chiselコンパイラ)
 [ FIRRTL 中間表現 (IR) ] ───> 各種最適化・型検証
              │
              ▼ (FIRRTLコンパイラ)
 [ ハードウェア記述ネットリスト ]
              │
              ├─> [ FPGA合成 (Vivado/Quartus) ] ──> ビットストリーム ──> 実機FPGA動作
              │
              └─> [ ASIC論理合成 (Design Compiler) ] ──> 配置配線 ──> シリコンウェハ製造
```

- Chiselは単なるシミュレータではなく、実機チップを製造するための本格的な設計言語。

<!-- Note:
ハードウェア製造への流れです。
皆さんが書いたChiselコードは、FIRRTLという中間表現を経て、論理合成ツールによって実際のトランジスタ回路へとマッピングされ、FPGAやシリコン半導体チップとしてこの世に誕生します。
皆さんの書いたコードが、物理的な電子の流れを支配しているのです。
-->

---

## 14. 最終品質チェックリスト

<div class="box">
<b>プロフェッショナル・デジタル設計者 チェックリスト</b><br>
□ 組合せ回路のすべての <code>Wire</code> にデフォルト値を与えているか？<br>
□ クロック信号に直接ゲート論理やリップル伝播を噛ませていないか？<br>
□ カウンタの多段接続は同期イネーブル (<code>cout</code> $\to$ <code>en</code>) で統一されているか？<br>
□ FSMの制御出力はグリッチのないMoore型で設計されているか？<br>
□ メモリの読み書き競合（RAW）に対するバイパス保護が考慮されているか？
</div>

<!-- Note:
最終チェックリストです。
この5つのチェック項目を常に意識して設計することで、誰に見せても恥ずかしくない、そして実機で絶対にバグを出さない堅牢なデジタルシステムを構築することができます。
-->

---

## 15. 本日の演習課題 (90分)

演習は**基本問題**と**発展問題**の2段階構成です。

### 基本問題（必須 / 目安45〜50分）
- **`RegisterFile8x8`**:
  - `Mem(8, UInt(8.W))` を用いたメモリ配列の宣言
  - `wen` によるクロック同期書き込み
  - `raddr1`, `raddr2` による2系統の独立した非同期読み出しポートの実装

### 発展問題（推奨 / 目安30〜35分）
- **`BypassedRegisterFile`**:
  - `RegisterFile8x8` のインスタンス化
  - 同一サイクルRAW競合の検出器と比較器の実装
  - MUXによる `wdata` の即座フォワーディング回路の検証

### バッファ (5〜10分)
- 全演習の完了確認、総括質疑応答

<!-- Note:
最後の演習の案内です。
まずは基本問題で2R1Wレジスタファイルを実装し、2つのポートから同時に正しいデータが読めることを確認してください。
そして発展問題でRAWフォワーディング回路を組み上げ、同じサイクルで書き込んだデータが即座に読み出せることをテストベンチで検証してください。
-->

---

## 16. 全8回 完走おめでとうございます！

### 講師からのメッセージ
デジタル回路の論理設計は、数学的な美しさと物理的な制約が交錯する最高にエキサイティングな工学分野です。
本講座で習得した確固たる同期設計原則と型安全なChisel記述力を武器に、ぜひオリジナルのプロセッサやアクセラレータの設計へと羽ばたいてください。

<br>

<div class="box" style="text-align: center; font-size: 24px; font-weight: bold;">
全8回 講義修了！ お疲れ様でした！<br>
最後の90分演習を全力で駆け抜けましょう！
</div>

<!-- Note:
全8回の講義、本当にお疲れ様でした。
皆さんが熱心に学ばれた8回の知識は、これからどのような高度なコンピュータシステムを設計する上でも、一生モノの頑丈な土台となります。
それでは、本講座最後の演習時間となります。starter_kitのExercises.scalaのLab08を開き、最高のレジスタファイルを完成させてください！
-->
