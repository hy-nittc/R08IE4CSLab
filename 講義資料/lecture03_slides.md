---
marp: true
theme: default
paginate: true
header: "第3回: 順序回路と基本カウンタ"
footer: "© 2026 Hideaki YANAGISAWA, Dept. of Computer Science and Electronic Engineering, NIT, Tokuyama College"
author: "Hideaki YANAGISAWA"
style: |
  section {
    font-family: 'Helvetica Neue', Arial, 'Hiragino Kaku Gothic ProN', sans-serif;
    font-size: 22px;
    padding: 32px 40px;
  }
  h1 { color: #1e3a8a; font-size: 34px; }
  h2 { color: #1e40af; border-bottom: 2px solid #3b82f6; font-size: 26px; }
  pre { font-size: 16px; margin: 8px 0; }
  .box { background: #eff6ff; border-left: 5px solid #3b82f6; padding: 8px 14px; margin: 8px 0; }
  .badge { background: #dbeafe; color: #1e40af; padding: 2px 6px; border-radius: 4px; font-weight: bold; font-size: 16px; }
---

# 第3回: 順序回路と基本カウンタ
### 〜クロック同期設計と同期リセット付きモジュロNカウンタ〜

<br>

- **資料作成者**: Hideaki YANAGISAWA
- **講義時間**: 60分
- **対象**: デジタル論理回路基礎習得者
- **本日のアジェンダ**:
  1. 順序回路の本質（フィードバックと状態記憶）
  2. クロック同期設計の鉄則とメタスタビリティ回避
  3. Chiselにおける暗黙のクロックとリセット
  4. レジスタプリミティブ (`Reg`, `RegInit`, `RegNext`)
  5. カウンタ回路の内部構成（加算器＋レジスタ＋比較器）
  6. モジュロ$N$カウンタのラップアラウンド論理
  7. 制御信号の優先度設計 (`clear` > `load` > `en`)
  8. 本日の演習課題（基本: モジュロNカウンタ / 発展: PWM発生器）

<!-- Note:
第3回の講義へようこそ。
第1回・第2回では「組合せ論理回路」を扱ってきました。
今回からはいよいよ、ハードウェアに「記憶」と「時間」の概念を導入する「順序回路（Sequential Circuit）」の世界に入ります。
現代のデジタルLSI設計の基礎である「完全同期設計」の原則を学び、Chiselがどのようにクロックとリセットを安全かつエレガントに抽象化しているかを理解していただきます。
-->

---

## 1. 順序回路 (Sequential Circuit) の本質

### 組合せ回路との本質的違い
出力ベクトル $\mathbf{Y}$ および次の状態 $\mathbf{S}_{next}$ が、**「現在の入力 $\mathbf{X}$」と「現在の内部状態 $\mathbf{S}_{current}$」の両方**によって決定される。

$$\mathbf{S}_{next} = f(\mathbf{X}, \mathbf{S}_{current})$$
$$\mathbf{Y} = g(\mathbf{X}, \mathbf{S}_{current})$$

```
          ┌───────────────────────────────────┐
          │                                   │ 状態帰還
          ▼                                   │
 ───> [ 次状態論理 ] ───> [ 状態レジスタ ] ───┴───> [ 出力論理 ] ───> 出力
入力   (組合せ回路)        (D-FF群: 同期)            (組合せ回路)
```

- **記憶性**: 過去の入力履歴の累積が「状態（State）」としてレジスタに保持される。

<!-- Note:
順序回路の基本モデルです。
フリップフロップという記憶素子を挟むことで、初めて「閉ループ（フィードバック）」を安全に組むことができるようになります。
クロックが来るたびに、次状態論理で計算された新しい値がレジスタに取り込まれ、回路の状態が1ステップ進みます。
-->

---

## 2. クロック同期設計 (Synchronous Design) の鉄則

### なぜ同期設計なのか？
回路内の**すべての記憶素子（FF）を単一の共通クロック（System Clock）の立ち上がりエッジ**で同時に駆動する設計手法。

<div class="box">
<b>非同期設計の危険性と同期設計のメリット</b><br>
・<b>非同期回路</b>: ゲート遅延のバラツキによるハザード、グリッチの誤記憶、メタスタビリティの伝播により設計・検証が極めて困難。<br>
・<b>同期設計</b>: クロック周期 $T_{clk}$ 内に組合せ回路の遅延が収まっていれば、途中のグリッチはエッジで完全に遮断され、確実な動作が保証される。
</div>

### セットアップ時間 ($t_{su}$) と ホールド時間 ($t_h$)
- **Setup Violation**: クロックエッジ直前にデータが安定していないと、FFが `0` でも `1` でもない中間電位（**メタスタビリティ**）に陥る。

<!-- Note:
同期設計の重要性です。
組合せ回路の出力には、ゲート遅延の差によって「ヒゲ（グリッチ）」と呼ばれる一瞬の不要なパルスが必ず発生します。
しかし、同期設計であれば、次のクロックエッジが来るまでに信号が安定していれば問題ありません。クロックエッジの瞬間に安定した値だけをサンプリングするため、途中のグリッチは綺麗に消去されます。
-->

---

## 3. Chiselにおける暗黙のクロックとリセット

Chiselでは、同期設計を強力に支援するため、**クロック (`clock`) と同期リセット (`reset`) が全てのモジュールに暗黙（Implicit）に配線**されます。

```scala
class MyModule extends Module {
  val io = IO(new Bundle { ... })
  // ここに clock や reset を手動で宣言・配線する必要はない！
  // Chisel環境が最上位から自動的にグローバルクロックツリーを供給する。
}
```

- **利点**:
  - ポート定義にクロックやリセットをいちいち書く手間が省ける。
  - 配線ミスによるクロックスキューやリセット漏れを構造的に防止。
  - 特殊な非同期クロック境界（CDC）を扱う場合のみ、明示的な Clock を使用。

<!-- Note:
Chiselの非常に優れた設計思想の一つがこの「暗黙のクロックとリセット」です。
一般的な同期回路では、すべてのFFに同じクロックとリセット線を引くのは自明の作業です。
Chiselでは、Moduleを継承するだけで、モジュール全体に自動的に共通のclockとresetが分配されます。
設計者はデータパスと制御論理の記述に100%集中することができます。
-->

---

## 4. Chiselのレジスタ生成: `RegInit`

ハードウェアのDフリップフロップ（D-FF）を生成するには、初期化付きレジスタ `RegInit` を使用します。

```scala
// リセット時に 0.U(8.W) で初期化される8ビットレジスタ
val countReg = RegInit(0.U(8.W))

// リセット時に false.B で初期化される1ビットフラグ
val flagReg  = RegInit(false.B)
```

### クロックエッジごとの更新
レジスタへの代入 `countReg := ...` は、**「次のクロック立ち上がりエッジでレジスタに取り込まれる次状態」**を意味します。

```scala
countReg := countReg + 1.U // 毎クロック1ずつカウントアップ
```

<div class="box">
<b>初期値のない <code>Reg</code> の使用禁止</b><br>
初期値のない <code>Reg(UInt(8.W))</code> は、電源投入時の値が未定義 (X) になります。実機での信頼性を保証するため、必ず <code>RegInit</code> でリセット初期値を明示してください。
</div>

<!-- Note:
レジスタの記述法です。
RegInit は「リセット時の初期値」を指定してD-FFを生成する構文です。
countReg := countReg + 1.U と書くと、ソフトウェアのループ変数のように見えますが、実際には「現在のレジスタ出力 countReg を加算器に通し、その出力をレジスタのD入力に接続している」というフィードバックループ回路を意味します。
-->

---

## 5. その他のレジスタ構文: `RegNext` とパイプライン

### `RegNext`: 1クロック遅延のショートカット
入力信号を1クロック遅延（サンプリング）させたい場合、`RegNext` が便利です。

```scala
// dataIn を1クロック遅延させた信号
val delayData = RegNext(io.dataIn, 0.U(8.W))
```

- 第二引数にリセット時の初期値を指定可能。

### パイプラインレジスタの役割
```
 [組合せ論理 A] ──> [ RegNext ] ──> [組合せ論理 B]
```
- 長大な組合せ論理の間にレジスタを挟むことで、クリティカルパスを半分に分割し、動作クロック周波数を2倍に引き上げることができる。

<!-- Note:
RegNext の紹介です。
信号を1クロックだけ遅らせたいとき、パイプラインステージを挿入したいときに1行で記述できます。
後ほど第5回の発展課題でパイプラインALUを作る際にも大活躍します。
-->

---

## 6. カウンタ回路 (Counter) の内部アーキテクチャ

カウンタは、クロックに同期して特定の数列を規則的に巡回する順序回路。

```
                    ┌─────────────────────────┐
                    │                         │ count (現在の値)
                    ▼                         │
   ───> [ 比較器 / 次状態論理 ] ───> [ レジスタ ] ──┴───> rollover (桁上げ)
  en        (ラップアラウンド)           (RegInit)
```

### カウンタの3大構成要素
1. **状態レジスタ (`RegInit`)**: 現在のカウント値を保持するD-FF群。
2. **インクリメンタ（加算器）**: `count + 1.U` を計算する組合せ回路。
3. **終端比較器 (Comparator)**: 最大値（例: $N-1$）に達したことを検出し、次サイクルで `0` に戻す論理。

<!-- Note:
カウンタの内部ブロック図です。
カウンタは一見単純ですが、順序回路の基本要素がすべて詰まっています。
加算器が次の値を計算し、比較器が上限を監視し、クロックが来るたびにレジスタが値を更新します。
この構造を理解することが、すべての複雑なFSMや制御回路を理解する鍵となります。
-->

---

## 7. モジュロ $N$ カウンタ (Modulo-$N$ Counter)

$0$ から $N-1$ までの $N$ 個の状態を巡回するカウンタ。

### ラップアラウンド（巡回）の論理条件
カウンタが $N-1$ に達したとき、次のクロックで `0` に戻らなければならない。

```scala
when(cntReg === (n - 1).U) {
  cntReg := 0.U
}.otherwise {
  cntReg := cntReg + 1.U
}
```

<div class="box">
<b>工学的注意: 比較演算子の選定</b><br>
大小比較 <code>cntReg &gt;= (n - 1).U</code> よりも、完全一致比較 <code>cntReg === (n - 1).U</code> の方が、合成されるコンパレータ回路のゲート段数が少なく高速です（ただしオーバーシュート保護が不要な場合）。
</div>

<!-- Note:
モジュロNカウンタの設計です。
0から数え始めるため、N進カウンタの最大値は N-1 です。例えば10進カウンタなら 0〜9 を数えます。
したがって、リセット判定は `=== (n - 1).U` になります。
初心者がよく `=== n.U` と書いてしまい、N+1進カウンタになってしまうミスが多いので注意してください。
-->

---

## 8. 制御信号の優先度設計

実用的なカウンタには、複数の制御入力（`clear`, `load`, `en`）が存在します。
これらの信号が**同時にアサート**された場合の優先順位を明確に定義しなければなりません。

### 業界標準の優先度アーキテクチャ
$$\mathbf{clear\ (最優先)} > \mathbf{load\ (第2優先)} > \mathbf{en\ (通常動作)}$$

```
 [loadData] ──┐
              ├── [MUX 2: load] ──┐
 [count + 1] ─┘        ▲          ├── [MUX 1: clear] ──> [次状態]
                      load        │          ▲
 0.U(初期値) ─────────────────────┘        clear
```

- **同期クリア (`clear`)**: どのような状態でも強制的に 0 に初期化する（安全装置）。
- **データロード (`load`)**: 任意の値からカウントを開始させる。
- **カウントイネーブル (`en`)**: カウントの進行 / 停止を制御。

<!-- Note:
制御信号の優先順位です。
ハードウェア制御において、リセットやクリアは安全に関わるため常に最優先されます。
その次に、外部から任意値を強制設定する load が優先され、通常動作である en は最も優先度が低くなります。
これを when / elsewhen の順序として記述します。
-->

---

## 9. モジュロ $N$ カウンタの完全なChisel記述

```scala
class ModuloNCounter(val n: Int, val width: Int) extends Module {
  val io = IO(new Bundle {
    val en       = Input(Bool())
    val clear    = Input(Bool())
    val load     = Input(Bool())
    val loadData = Input(UInt(width.W))
    val count    = Output(UInt(width.W))
    val rollover = Output(Bool())
  })

  val cntReg = RegInit(0.U(width.W))

  when(io.clear) {
    cntReg := 0.U
  }.elsewhen(io.load) {
    cntReg := Mux(io.loadData < n.U, io.loadData, 0.U)
  }.elsewhen(io.en) {
    cntReg := Mux(cntReg === (n - 1).U, 0.U, cntReg + 1.U)
  }

  io.count    := cntReg
  io.rollover := io.en && (cntReg === (n - 1).U)
}
```

<!-- Note:
本日の基本問題のコア実装です。
入出力BundleとRegInit、そして優先度付きwhen構文が見事に噛み合っています。
最後の rollover 信号に注目してください。
rollover は count が n-1 であり、かつ en が true である瞬間に 1 クロック幅のパルスとして出力されます。
このパルスが、第6回で扱う多段カウンタのカスケード接続のキー信号になります。
-->

---

## 10. `rollover` 出力のタイミング設計

### 組合せ出力 vs レジスタ出力
`io.rollover` は、カウンタが $N-1$ に達している**そのサイクル中**にアサートされる組合せ信号です。

```
CLK      : ───┐   ┌───┐   ┌───┐   ┌───┐   ┌───┐   ┌───
              └───┘   └───┘   └───┘   └───┘   └───┘   
count    : ──X N-2 X       N-1       X    0    X  1  X
rollover : ─────────────────|‾‾‾‾‾‾‾‾‾|───────────────
                            (1サイクルのみHigh)
```

- **メリット**: 次のクロックの立ち上がりで、上位カウンタが同時にインクリメントできる（同期カスケード動作）。
- **注意点**: `cntReg` が $N-1$ でも、`en` が Low なら `rollover` を出してはならない（`io.en && ...`）。

<!-- Note:
タイミングチャートによる解説です。
N-1 の状態の時に rollover が High になります。
次のクロックエッジで、自分自身は 0 にクリアされ、同時にこの rollover をイネーブルとして受け取った後段のカウンタが 1 カウントアップします。
この1サイクルの重複が、完全同期カスケードを実現する秘訣です。
-->

---

## 11. 発展課題: パルス幅変調 (PWM) 発生器

### PWM (Pulse Width Modulation) とは
一定の周期 $T$ の中で、パルスが High である時間（デューティ比 $Duty$）を変化させる制御方式。

```
周期 T ───────────────────────────────>
┌──────────────┐                      ┌──────────────┐
│  High (Duty) │      Low             │  High (Duty) │
┘              └──────────────────────┘              └────
```

- **用途**: モータの回転数制御、LEDの無段階輝度調光、D級アンプ。
- **ハードウェア構成**:
  - **周期カウンタ**: $0 \sim Period$ を巡回カウントするモジュロカウンタ。
  - **デジタルコンパレータ**: カウント値と設定された $Duty$ レジスタを比較。

<!-- Note:
発展課題で作成するPWMパルス発生器の解説です。
モータ制御やLEDの調光で日常的に使われているPWMは、まさにモジュロカウンタと比較器の組み合わせそのものです。
周期カウンタが走っており、その値がデューティレジスタより小さい間だけ出力をHighにします。
-->

---

## 12. PWM発生器の回路ブロック図

```
 period ──┐
          ├── [ カウンタ比較器 ] ──┐
 cntReg ──┘                        ▼
                              [ クリア ]
                                   │
                                   ▼
          ┌─────────────────> [ cntReg ] (0〜period)
          │                        │
          │                        ├──┐
 duty ────┼────────────────────────┼──┼──> [ < 比較器 ] ──> pwmOut
          │                        │  │        (cnt < duty)
 en ──────┴────────────────────────┴──┘
```

- $cntReg < duty$ ならば $pwmOut = 1$
- $cntReg \ge duty$ ならば $pwmOut = 0$
- $duty = 0$ のときは完全消灯 (常時 0)、$duty \ge period$ のときは常時点灯 (常時 1)。

<!-- Note:
PWM発生器の内部ブロック図です。
period 入力によってPWMの基本周波数を設定でき、duty 入力によってパルス幅（オン時間）を0%〜100%まで動的に変更できます。
基本問題のカウンタをどのように応用するかを示す格好の題材です。
-->

---

## 13. PWMのコーナーケース処理

デジタル回路の発展課題では、**境界条件（エッジケース）**の処理が品質を左右します。

### 検討すべきコーナーケース
1. **$duty == 0$ の場合**:
   - カウント値は常に $\ge 0$ であるため、単純に $cnt < duty$ と比較すると意図通り常時 0 が出力される。
2. **$duty > period$ の場合**:
   - デューティ比が100%を超える設定値が入力された場合、出力は常時 1 (High) に固定されるべき。
3. **$en == 0$ (ディセーブル時)**:
   - カウンタは直ちに停止し、出力は安全のため強制的に 0 に落とす。

<!-- Note:
エッジケースへの配慮です。
ハードウェア設計では、入力に想定外の値が入った場合でも回路がハングアップしたり誤動作しないようにガードを設けることが不可欠です。
特に en が落ちたときにモータが回り続けないよう、出力を false.B に落とす安全設計を意識してください。
-->

---

## 14. 順序回路のデバッグと検証手法

### 波形確認の着眼点
1. **リセット解除直後の挙動**: クロックエッジで正しく `RegInit` の値に初期化されているか。
2. **遷移の1サイクル遅延**: 組合せ回路と違い、入力の変化が出力レジスタに反映されるのは**「次のクロックの立ち上がり」**である。
3. **グリッチ耐性**: 内部の組合せ演算が途中で揺れても、サンプリング点（セットアップ時間前）で静定していれば波形は乱れない。

<!-- Note:
デバッグの心構えです。
受講者が順序回路で最も混乱しやすいのが「1クロックの遅延」です。
入力ピンの信号を変えても、その瞬間に出力レジスタは変わりません。次のクロックエッジが来て初めて値が更新されます。
シミュレータの波形を見る際は、クロックの立ち上がりエッジに縦線を引いて、エッジの前後の値を観察してください。
-->

---

## 15. 本日の演習課題 (90分)

演習は**基本問題**と**発展問題**の2段階構成です。

### 基本問題（必須 / 目安45〜50分）
- **`ModuloNCounter`**:
  - パラメータ $N$ 進、ビット幅 $width$
  - `clear` > `load` > `en` の優先度制御
  - 端数検出 $N-1$ でのラップアラウンドと `rollover` パルス出力

### 発展問題（推奨 / 目安30〜35分）
- **`PwmGenerator`**:
  - 周期レジスタ `period` とデューティ比レジスタ `duty` の動的制御
  - コーナーケース（0%および100%）の正確なハンドリング

### バッファ (5〜10分)
- コード整形、テスト実行、質疑応答

<!-- Note:
演習課題の概要です。
まずは ModuloNCounter を完璧に動かしてください。このカウンタは第6回の60進カウンタでもそのままサブモジュールとして活用します。
余力のある方は PWM パルス発生器に挑戦し、周期カウンタと比較器の連動を体験してください。
-->

---

## 16. 第3回 まとめ & 次回予告

### 本日の重要ポイント
1. 順序回路は「次状態組合せ論理」と「状態保持D-FF」のフィードバック結合である。
2. Chiselは `Module` 内に**暗黙の `clock` と `reset`** を自動配線する。
3. リセット初期値付きレジスタは `RegInit(...)` で生成する。
4. モジュロ$N$カウンタは $N-1$ でラップアラウンドし、`rollover` パルスを出力する。

### 次回予告: 第4回「RingカウンタとJohnsonカウンタ」
- シフトレジスタの循環構造と1-hot状態遷移
- Johnsonカウンタ（$2N$状態巡回）と反転帰還
- 電源投入時の外乱に対する**自己復帰論理 (Self-Correction Logic)**

<!-- Note:
第3回の講義は以上となります。
順序回路とカウンタの設計は、すべてのデジタルハードウェアエンジニアにとっての通過儀礼です。
それでは90分の演習に移ります。starter_kitのExercises.scalaのLab03を開いてください。
-->
