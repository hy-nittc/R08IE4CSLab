# 全8回 講師用トークスクリプト・進行ガイド (Speaker Notes)

- **資料作成者**: Hideaki YANAGISAWA
- **授業名**: 「Computer System Laboratory」
- **時間設定**: 各回 計150分（**講義 60分 + 演習 90分**）
- **構成**: 各回のタイムライン配分、導入、本論での強調点、演習ガイダンス、質疑対応ポイント

---

## 講義標準タイムライン (60分)
- **00〜05分 (5分)**: 導入・前回復習・本日の到達目標（スライド1〜2）
- **05〜25分 (20分)**: 本論パート1: 回路理論・アーキテクチャ・ブロック図（スライド3〜8）
- **25〜50分 (25分)**: 本論パート2: Chisel構文・イディオム・工学的注意点（スライド9〜14）
- **50〜60分 (10分)**: まとめ・演習課題（基本・発展）の要件と着眼点解説（スライド15〜16）

## 演習標準タイムライン (90分)
- **00〜05分 (5分)**: 開発環境確認・スケルトンコード確認
- **05〜50分 (45分)**: 【基本問題（必須）】の実装と単体テスト検証
- **50〜85分 (35分)**: 【発展問題（推奨）】への挑戦とコーナーケース検証
- **85〜90分 (5分)**: 模範解答レビュー・コード整形・質疑応答

---

## 第1回: 組合せ回路の基礎 (講義60分 + 演習90分)
- **強調点**:
  - 信号接続記号 `:=` は物理配線であり、有向非巡回グラフ (DAG) を形成すること。閉ループの絶対禁止。
  - 通常加算 `+` では最上位キャリーが切り捨てられるため、桁あふれを保持するには拡張加算 `+&` が必須であること。
- **講師トーク要約**:
  「第1回はすべての基礎となる組合せ回路です。Chiselでは型システム `Bool`, `UInt`, `SInt` と `.W` を用いて、ビット幅の不一致をコンパイル時に完全に防ぎます。演習ではまず1ビット全加算器を作り、それを直列接続して4ビット加算器を組み上げます。発展問題では加減算器とオーバーフロー検出フラグの実装に挑戦していただきます。」
- **演習巡回ポイント**:
  - `RippleCarryAdder4` で全加算器のキャリー出力が次の全加算器のキャリー入力へ正しく伝播しているか確認。
  - 発展問題で `sub` 信号による 2の補数反転（`b ^ Fill(4, sub)`）とキャリーインへの `sub` 供給の理解度をチェック。

---

## 第2回: Muxとデコーダの回路 (講義60分 + 演習90分)
- **強調点**:
  - 未完全記述による意図しない透過ラッチの発生メカニズムと、`WireDefault` による撲滅。
  - `when / elsewhen`（優先度連鎖）と `switch / is`（直交並列デコード）の合成トポロジーの相違。
- **講師トーク要約**:
  「第2回はマルチプレクサとデコーダです。ハードウェア設計において最も恐ろしいバグの一つが『意図しない透過ラッチ』です。条件分岐で値を代入し忘れた信号線は、前回の値を記憶しようとして勝手にラッチになってしまいます。これを防ぐ唯一絶対の作法が `WireDefault` です。演習では、ラッチのない7セグデコーダと優先度付きエンコーダを作成します。」
- **演習巡回ポイント**:
  - `SevenSegDecoder` で `WireDefault(0.U(7.W))` が宣言されているか。未定義値（10〜15）で消灯するか。
  - 発展問題で `io.in.orR` を用いて `valid` フラグが簡潔に生成されているか。

---

## 第3回: 順序回路と基本カウンタ (講義60分 + 演習90分)
- **強調点**:
  - 完全同期設計原則（単一クロックで全FFを駆動し、グリッチとメタスタビリティを遮断）。
  - Chiselの暗黙クロック・同期リセットと `RegInit`。
  - 制御信号の優先順位: $\mathbf{clear} > \mathbf{load} > \mathbf{en}$。
- **講師トーク要約**:
  「第3回から回路に『時間と記憶』が加わります。Chiselでは `Module` を継承するだけで、グローバルクロックと同期リセットが自動配線されます。皆さんは `RegInit` を使ってリセット初期値を明示するだけで、安全なD-FFが手に入ります。演習では任意進数のモジュロNカウンタと、周期カウンタを活用したPWM発生器を設計します。」
- **演習巡回ポイント**:
  - `ModuloNCounter` で $N-1$ でラップアラウンドしているか（$N$ になっていないか）。
  - `rollover` パルスが $N-1$ かつ `en` が有効な1クロックの間だけ High になっているか。

---

## 第4回: RingカウンタとJohnsonカウンタ (講義60分 + 演習90分)
- **強調点**:
  - シフトレジスタの循環による加算器レス超高速動作と1-hot状態。
  - Johnsonカウンタの $2N$ 状態巡回と2入力ANDによるグリッチフリーデコード。
  - 実機ノイズによる未使用状態トラップと、`PopCount` を用いた自己復帰回路の設計。
- **講師トーク要約**:
  「第4回はシフトレジスタを応用した特殊カウンタです。Ringカウンタは加算器もデコーダも使わないため、最高クロック周波数で動きます。しかし、ノイズで一度でも状態が化けると二度と戻れないという致命的リスクを抱えています。そこで『自己復帰論理』を組み込みます。発展問題では1ビットずつしか動かないグレイコードカウンタに挑戦します。」
- **演習巡回ポイント**:
  - `SelfCorrectingRingCounter` で、初期値以外に手動で不正な値（例: 0や3）を注入した際に1サイクルで復帰するか。
  - `GrayCodeCounter` で隣接遷移のハミング距離が厳密に1になっているか。

---

## 第5回: モジュールの構造化設計 (講義60分 + 演習90分)
- **強調点**:
  - 大規模回路の分割統治 (Divide and Conquer) とカプセル化。
  - `val sub = Module(new SubModule)` によるインスタンス化とポートバインド。
  - 出力段レジスタ挿入（パイプライン化）によるクリティカルパス分断と動作周波数向上。
- **講師トーク要約**:
  「第5回は構造化設計です。数百万ゲートのチップを1つのファイルに書く人はいません。機能ごとにモジュールを作り、それらを上位で配線します。本日は算術ユニットと論理ユニットを別々に設計し、上位モジュールでセレクタと結合して本格的なALUを完成させます。発展課題ではパイプラインレジスタとフラグ生成回路を追加します。」
- **演習巡回ポイント**:
  - `Module(...)` の記述漏れがないか。
  - 親モジュールの入力から子モジュールの入力へ、子モジュールの出力から親モジュールの出力へという信号の向きが正しく配線されているか。

---

## 第6回: 60進カウンタの構造化設計 (講義60分 + 演習90分)
- **強調点**:
  - **非同期リップルクロックの完全排除**（下位のキャリーを上位のクロック端子に繋ぐのは絶対に禁止）。
  - 単一クロック駆動と同期イネーブル伝播 (`cout` $\to$ `en`) による完全同期カスケード動作。
- **講師トーク要約**:
  「第6回は構造化設計の応用として、時計の秒・分を数える60進カウンタを作ります。10進カウンタと6進カウンタを組み合わせます。ここで最も大切なルールは『非同期クロックを絶対に使わないこと』です。すべてのFFに同じクロックを与え、10進カウンタの桁上げを6進カウンタのイネーブルに繋ぎます。発展課題では24時間時計コアを完成させます。」
- **演習巡回ポイント**:
  - 10進カウンタの `rollover` と外部 `en` のANDが6進カウンタの `en` に繋がっているか。
  - 発展課題の `DigitalClockCore` で 23:59:59 の次の秒パルスで 00:00:00 に正しく戻るか。

---

## 第7回: 有限状態機械 (FSM) (講義60分 + 演習90分)
- **強調点**:
  - 制御部の中核をなすFSMの3大構成（状態レジスタ、次状態論理、出力論理）。
  - **Moore型 vs Mealy型**: なぜMoore型は出力がグリッチフリーなのか。
  - `ChiselEnum` による型安全な状態定義と、3プロセス分離記述スタイル。
- **講師トーク要約**:
  「第7回はハードウェア制御の頭脳であるFSMです。通信プロトコルやCPU制御などあらゆる場面で使われます。本講義では、出力に入力グリッチが突き抜けない高信頼な『Moore型FSM』を徹底推奨します。基本問題では連続ビット "101" の検出器、発展問題ではタイマと歩行者割り込みが連動した交通信号機コントローラを作ります。」
- **演習巡回ポイント**:
  - "10101" のような重複パターンが来たときに、パルスが2回正しく出るか。
  - 出力 `detected` が状態レジスタのみから生成されているか（Moore型の遵守）。

---

## 第8回: メモリ回路と非同期読み出し (講義60分 + 演習90分)
- **強調点**:
  - 記憶素子の階層構造と `Mem`（非同期読み出し）/ `SyncReadMem`（同期読み出し）の使い分け。
  - 2読み出し・1書き込みポート (2R1W) レジスタファイルの構成。
  - 同一サイクルでの同一アドレス同時アクセスに対する **RAWフォワーディング回路**。
- **講師トーク要約**:
  「最終回はメモリ回路です。CPUの汎用レジスタファイルのように、アドレスを与えた瞬間に同一サイクルでデータを読み出したい場合は `Mem` を使用します。ここで生じる重大な問題がRAW競合です。書き込み中のデータを同じサイクルで読もうとすると未定義値になります。発展課題では、書き込みデータを直接読み出しポートへ迂回させるフォワーディング回路を完成させます。」
- **演習巡回ポイント**:
  - `raddr1` と `raddr2` の2つのポートから独立して非同期にデータが読めているか。
  - 発展課題で、`wen && (waddr === raddr)` のときに `wdata` が即座にフォワーディングされているか。
