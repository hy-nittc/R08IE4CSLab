---
marp: true
theme: default
paginate: true
header: "第4回: RingカウンタとJohnsonカウンタ"
footer: "© 2026 Hideaki YANAGISAWA, Dept. of Computer Science and Electronic Engineering, NIT, Tokuyama College"
author: "Hideaki YANAGISAWA"
style: |
  section {
    font-family: 'Helvetica Neue', Arial, 'Hiragino Kaku Gothic ProN', sans-serif;
    font-size: 22px;
    padding: 32px 40px;
  }
  h1 { color: #1e3a8a; font-size: 34px; }
  h2 { color: #1e40af; border-bottom: 2px solid #3b82f6; font-size: 26px; }
  pre { font-size: 16px; margin: 8px 0; }
  .box { background: #eff6ff; border-left: 5px solid #3b82f6; padding: 8px 14px; margin: 8px 0; }
  .badge { background: #dbeafe; color: #1e40af; padding: 2px 6px; border-radius: 4px; font-weight: bold; font-size: 16px; }
---

# 第4回: RingカウンタとJohnsonカウンタ
### 〜シフトレジスタの循環構造と自己復帰型シーケンス回路〜

<br>

- **資料作成者**: Hideaki YANAGISAWA
- **講義時間**: 60分
- **対象**: デジタル論理回路基礎習得者
- **本日のアジェンダ**:
  1. 前回の復習（バイナリカウンタの限界とデコード遅延）
  2. シフトレジスタの基本構造とChiselでの配線 (`Cat`, ビットスライス)
  3. Ringカウンタのアーキテクチャと1-hot（ワンホット）状態
  4. Johnsonカウンタ（モビウス・カウンタ）の $2N$ 状態巡回
  5. 不正状態へのトラップと**自己復帰論理 (Self-Correction Logic)**
  6. 発展トピック: グレイコード・カウンタ（ハミング距離1遷移）
  7. 本日の演習課題（基本: 自己復帰Ring & Johnson / 発展: グレイコード）

<!-- Note:
第4回講義へようこそ。
前回は二進数を数え上げるバイナリカウンタを学びました。
今回は、同じカウンタでも全く異なるアプローチを取る「シフトレジスタ応用カウンタ」を扱います。
RingカウンタやJohnsonカウンタは、加算器を使わずにシフトと帰還だけで状態を巡回させるため、超高速動作や低消費電力、グリッチフリーなタイミング信号生成に広く使われています。
さらに、実ハードウェアで致命傷となる「迷走状態からの自動復帰」を設計します。
-->

---

## 1. バイナリカウンタの課題と特殊カウンタの意義

### バイナリカウンタ（前回復習）の特性
- $N$ 個のフリップフロップで $2^N$ 個の状態を表現（回路密度の最大化）。
- **デメリット**:
  1. 状態判定に**多入力デコーダ（ANDゲート網）**が必要 $\to$ 遅延と電力の増大。
  2. 複数ビットが同時に反転する（例: $0111 \to 1000$ では4ビット全反転）ため、**電源ノイズ（同時スイッチングノイズ: SSN）**と**出力グリッチ**が発生しやすい。

### シフトレジスタ応用カウンタの登場
加算器とデコーダを排除し、**「レジスタのシフト動作」と「出力の帰還」のみ**でシーケンスを生成する。

<!-- Note:
バイナリカウンタの弱点についての工学的解説です。
バイナリカウンタはFFの数を最小化できますが、0111から1000に変わるときのように、全ビットが一斉に反転する瞬間があります。
このとき急激な貫通電流が流れて電源電圧が揺れ、また各ビットの反転タイミングの微妙なズレが原因でデコード出力に激しいヒゲ（グリッチ）が出ます。
高速通信やタイミング制御では、ビット反転数が少なくデコーダが不要な特殊カウンタが求められます。
-->

---

## 2. シフトレジスタ (Shift Register) の基礎

フリップフロップを直列に接続し、クロック毎にデータを隣の段へ転送する回路。

```
       ┌───────┐     ┌───────┐     ┌───────┐     ┌───────┐
in ───>│ FF(3) │────>│ FF(2) │────>│ FF(1) │────>│ FF(0) │───> out
       └───────┘     └───────┘     └───────┘     └───────┘
           ▲             ▲             ▲             ▲
          CLK           CLK           CLK           CLK
```

### Chiselにおける左シフトと右シフトの記述
ビットスライスと `Cat` を用いて、ハードウェア配線を直接表現します。

```scala
val shiftReg = RegInit(0.U(4.W))

// 左シフト (MSBを捨て、LSBに inBit を挿入)
shiftReg := Cat(shiftReg(2, 0), io.inBit)

// 右シフト (LSBを捨て、MSBに inBit を挿入)
shiftReg := Cat(io.inBit, shiftReg(3, 1))
```

<!-- Note:
シフトレジスタの基本構造です。
Chiselでは、Cat(shiftReg(2, 0), inBit) と書くだけで、下位3ビットを取り出して1ビット左にずらし、空いた最下位ビットにinBitを入れるという配線が1行で書けます。
加算器などの演算器を一切消費しない、純粋な配線とレジスタだけの低遅延回路です。
-->

---

## 3. Ringカウンタ (Ring Counter)

シフトレジスタの最終段出力（MSB）を、先頭段入力（LSB）にそのまま循環接続した回路。

```
     ┌────────────────────────────────────────────────────────┐
     │                                                        │ 循環帰還
     ▼                                                        │
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐│
│  FF(0)  │─────>│  FF(1)  │─────>│  FF(2)  │─────>│  FF(3)  │┘
└─────────┘      └─────────┘      └─────────┘      └─────────┘
```

### 1-hot（ワンホット）符号状態遷移
$N$ 個のFFのうち、**常に1つのビットだけが `1` (High) で、他はすべて `0`**。

$$0001_2 \longrightarrow 0010_2 \longrightarrow 0100_2 \longrightarrow 1000_2 \longrightarrow 0001_2 \cdots$$

- 状態数: $N$ 個（4ビットなら4状態）。
- リセット初期値: 必ず `1.U` (`0001_2`) 等の1-hot値で初期化する。

<!-- Note:
Ringカウンタの動作です。
4ビットの場合、初期値として 0001 を入れておくと、クロックが来るたびに 1 が右から左へとぐるぐる巡回します。
常に1つのビットだけが1である表現を「1-hot（ワンホット）」と呼びます。
-->

---

## 4. Ringカウンタの工学的利点

### 1. 完全デコーダフリー (No Decoding Logic)
現在の状態を知るためにANDゲート等のデコーダ回路が一切不要。
- レジスタ出力ピン `out(0)` がそのまま「フェーズ0フラグ」になる。
- ゲート遅延ゼロで各ステージのイネーブル信号を取り出せる。

### 2. 超低消費電力 & 超高速クロック動作
- 帰還経路に組合せ論理ゲートが一切挟まらないため、クリティカルパス遅延は FFの $t_{co}$ (Clock-to-Output) + セットアップ時間 $t_{su}$ のみ。
- プロセッサのCPU命令実行フェーズ（Fetch $\to$ Decode $\to$ Execute $\to$ Writeback）のタイミングジェネレータに最適。

<!-- Note:
Ringカウンタの絶大なメリットです。
バイナリカウンタで「今フェーズ3か？」を判定するには、bit0とbit1のANDゲートを通す必要がありますが、Ringカウンタならout(3)のピンから直接信号を取るだけで済みます。
遅延はゼロ、ゲートによる電力消費もゼロです。CPUのマイクロシーケンサなどで極めて多用されます。
-->

---

## 5. Johnsonカウンタ（モビウス・カウンタ）

シフトレジスタの最終段出力を**「反転 (NOT)」**させて先頭段へ帰還させた回路。

```
     ┌────────────────────────────────────────────────────────┐
     │                                                        │
     ▼                                                        │
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐│
│  FF(0)  │─────>│  FF(1)  │─────>│  FF(2)  │─────>│  FF(3)  │┼──> [ NOT ] ──┐
└─────────┘      └─────────┘      └─────────┘      └─────────┘               │
     ▲                                                                       │
     └───────────────────────────────────────────────────────────────────────┘
```

### 状態遷移シーケンス (4ビット: 8状態)
$$0000 \to 0001 \to 0011 \to 0111 \to 1111 \to 1110 \to 1100 \to 1000 \to 0000$$

- 状態数: **$2 \times N$ 個**（4ビットで 8状態、Ringカウンタの2倍！）。
- リセット初期値: 通常 `0000_2` で初期化。

<!-- Note:
Johnsonカウンタ（別名モビウス・カウンタ）です。
出力の最上位ビットをNOTゲートで反転させて最下位に戻します。
すると、まず1が順番に充填されていき、全ビットが1になったら今度は0が順番に充填されていくという、モビウスの輪のような2N個の状態を巡回します。
FFが4個で8個の状態を作れるため、Ringカウンタより回路効率が2倍になります。
-->

---

## 6. Johnsonカウンタのデコード特性

### 2入力ANDゲートだけで全状態を完全デコード可能
Johnsonカウンタの任意の状態は、たった「2つのビットの論理積」だけで一意にデコードできます。

| 状態番号 | 4ビットパターン | デコード論理式 |
| :---: | :---: | :--- |
| **S0** | `0000` | $\overline{Q_3} \cdot \overline{Q_0}$ |
| **S1** | `0001` | $\overline{Q_1} \cdot Q_0$ |
| **S2** | `0011` | $\overline{Q_2} \cdot Q_1$ |
| **S3** | `0111` | $\overline{Q_3} \cdot Q_2$ |
| **S4** | `1111` | $Q_3 \cdot Q_0$ |
| **S5** | `1110` | $Q_1 \cdot \overline{Q_0}$ |
| **S6** | `1100` | $Q_2 \cdot \overline{Q_1}$ |
| **S7** | `1000` | $Q_3 \cdot \overline{Q_2}$ |

- バイナリカウンタ（4入力ANDゲートが必要）に比べ、デコード遅延が極小。
- 隣接状態間で**反転するビットは常に1ビットのみ** $\implies$ グリッチフリー！

<!-- Note:
Johnsonカウンタの素晴らしい特性です。
8つの状態のデコードが、すべて「2入力AND」で済みます。
しかも、状態が遷移するときに変化するビットは常に1ビットだけです。
同時に2つ以上のビットが変わらないため、デコード出力にスパイク状のグリッチが原理的に発生しません。
-->

---

## 7. 【実機の脅威】不正状態へのトラップ問題

### 未使用状態 (Unused States) の存在
- 4ビットのレジスタは物理的に $2^4 = 16$ 通りの状態を持ちうる。
  - **Ringカウンタ**: 正常状態 4個 / **未使用状態 12個**
  - **Johnsonカウンタ**: 正常状態 8個 / **未使用状態 8個**

<div class="box">
<b>実回路で何が起きるか？</b><br>
電源投入時のリセット不良、外乱サージ、静電気ノイズ、宇宙線中性子によるソフトエラー等により、FFの値が1ビット反転して「未使用状態」に飛んでしまうことがある！
</div>

### 死のループ (Lock-up)
通常の循環帰還だけでは、未使用状態同士の別の閉ループに閉じ込められ、**二度と正常なシーケンスに戻れなくなる（システムハングアップ）**。

<!-- Note:
現場のエンジニアが直面する非常に深刻なハードウェアトラブルです。
机上のシミュレーションでは初期値から綺麗に回っていても、実機の基板上ではノイズ一発で 0001 が 0011 に化けることがあります。
通常のRingカウンタで 0011 になると、0011 -> 0110 -> 1100 -> 1001 -> 0011 ... と、2ビット立っているループを永久に回り続け、二度と 0001 に戻れません。これを「Lock-up」と呼びます。
-->

---

## 8. 自己復帰論理 (Self-Correction Logic)

回路が万が一不正状態に迷い込んだ場合、**外部リセットをかけずとも自律的に正常状態へ復帰**する保護回路。

```
 [現在の状態] ───> [ 正常性検査論理 ]
                          │
         不正検出 (エラー) │
                          ▼
            [ 次クロックで強制的に初期値 (0001) をロード ]
```

### Chiselにおける正常性の判定
Ringカウンタの場合、「立っているビットの総数 (PopCount)」が厳密に $1$ であるかを検査する。

```scala
import chisel3.util.PopCount

val isOneHot = PopCount(stateReg) === 1.U
```

- `PopCount` はハードウェアの加算器ツリーとして合成される。

<!-- Note:
自己復帰回路の設計方針です。
Chiselには便利な PopCount（Population Count: 1であるビットの数を数える関数）があります。
これを使って `PopCount(stateReg) === 1.U` を評価します。
もし立っているビット数が0個（全ゼロ）、あるいは2個以上あれば、即座に不正と判定し、次クロックで強制的に 0001 を再ロードします。
-->

---

## 9. 自己復帰Ringカウンタの完全実装

```scala
class SelfCorrectingRingCounter(val width: Int = 4) extends Module {
  val io = IO(new Bundle {
    val en  = Input(Bool())
    val out = Output(UInt(width.W))
  })

  val state = RegInit(1.U(width.W)) // 0001_2

  when(io.en) {
    val isOneHot = PopCount(state) === 1.U
    when(!isOneHot) {
      // 不正状態からの自動自己復帰
      state := 1.U(width.W)
    }.otherwise {
      // 正常な1-hot左循環シフト
      state := Cat(state(width - 2, 0), state(width - 1))
    }
  }

  io.out := state
}
```

- ノイズ等で状態が化けても、最大1クロックで正常ループに自己治癒する。

<!-- Note:
本日の基本問題の実装コードです。
stateRegが正常なら通常通り左循環シフトし、もしノイズで化けたら次のクロックで即座に 1.U へ復帰します。
宇宙航空や医療機器、自動車ECUなど、高い信頼性が要求されるミッションクリティカルなハードウェアでは必須の設計技法です。
-->

---

## 10. 発展課題: グレイコード・カウンタ (Gray Code Counter)

### グレイコード（交番2進符号）とは
隣接するどの2つの数値の間でも、**変化するビットが常に「厳密に1ビットだけ」**である特殊な2進数コード。

<div style="display: flex; justify-content: space-between;">
<div style="width: 48%;">

### 通常の2進数 (4bit)
- $0000 \to 0001$ (1bit変化)
- $0001 \to 0010$ (2bit変化)
- $0011 \to 0100$ (3bit変化)
- $0111 \to 1000$ (**4bit全変化**)

</div>
<div style="width: 48%;">

### グレイコード (4bit)
- $0000 \to 0001$ (1bit)
- $0001 \to 0011$ (1bit)
- $0011 \to 0010$ (1bit)
- $0010 \to 0110$ (1bit)
- 常にハミング距離 $= 1$

</div>
</div>

- **用途**: ロータリーエンコーダ（角度センサ）、非同期FIFOのポインタ受け渡し。

<!-- Note:
発展課題で取り組むグレイコード・カウンタです。
通常のバイナリでは、0111から1000に変わる際、4つのビットが完全に同時に反転することは物理的にあり得ず、過渡的に 0110 や 1111 などの偽のコードが一瞬出力されてしまいます。
グレイコードなら常に1ビットしか動かないため、非同期回路境界をまたぐ通信で読み取りミスが絶対に起きません。
-->

---

## 11. バイナリ $\iff$ グレイコードの相互変換論理

### バイナリコード $B$ から グレイコード $G$ への変換
第 $i$ ビットのグレイコード $G_i$ は、バイナリの自身と1つ上位ビットの排他的論理和。

$$G_i = B_i \oplus B_{i+1}$$
$$\mathbf{G} = \mathbf{B} \oplus (\mathbf{B} \gg 1)$$

```scala
val gray = bin ^ (bin >> 1) // わずか1行で変換可能！
```

### ハードウェア構成
```
 bin(3) ───────────────────────────────┬──> gray(3)
                                       │
 bin(2) ───────────────┬──> [ XOR ] ───┴──> gray(2)
                       │
 bin(1) ───────┬──> [ XOR ] ──────────────> gray(1)
               │
 bin(0) ──> [ XOR ] ──────────────────────> gray(0)
```

<!-- Note:
バイナリからグレイコードへの変換アルゴリズムです。
数式で見ると難しそうに見えますが、ハードウェア的には「自分自身と、1ビット右シフトした値のXORを取る」だけです。
Chiselでは `bin ^ (bin >> 1)` と書くだけで、図のような並列XORゲートアレイが瞬時に合成されます。
-->

---

## 12. グレイコード・カウンタのアーキテクチャ

内部に通常のバイナリカウンタを保持し、出力段でグレイコードへ変換するのが最も面積効率と速度に優れます。

```
 [en, clear] ──> [ 内部バイナリカウンタ ] (binReg) ───┬───> io.bin
                         (RegInit)                     │
                                                       ▼
                                              [ G = B ^ (B >> 1) ]
                                                       │
                                                       ▼
                                                    io.gray
```

- 内部レジスタはシンプルな `binReg + 1.U` でインクリメント。
- 出力段の1段のXORゲートアレイを経て、常にハミング距離1の `gray` を出力。

<!-- Note:
グレイコードカウンタの設計手法です。
グレイコードのまま次の値を計算する次状態論理を組むことも可能ですが、式が非常に複雑になります。
実務では、内部レジスタを標準バイナリカウンタとして進め、その出力を1段のXOR回路を通してグレイコードとして取り出す方式が最も標準的で信頼性が高くなります。
-->

---

## 13. カウンタ回路方式の比較と選定ガイドライン

| 特性 | バイナリカウンタ | Ringカウンタ | Johnsonカウンタ | グレイコード |
| :--- | :---: | :---: | :---: | :---: |
| **FF数 ($N$状態)** | $\lceil \log_2 N \rceil$ | $N$ | $N / 2$ | $\lceil \log_2 N \rceil$ |
| **デコーダ回路** | $N$入力AND | **不要 (0ゲート)** | 2入力AND | 複雑 |
| **反転ビット数** | 最大 $N$ ビット | 2ビット | **1ビット** | **1ビット** |
| **最大動作周波数** | 中 (加算器律速) | **極高 (配線のみ)** | **極高** | 高 |
| **耐ノイズ性** | グリッチ発生 | 不正状態対策必須 | 不正状態対策必須 | **グリッチ皆無** |

<!-- Note:
4種類のカウンタの比較表です。
受講者の皆さんが将来ハードウェアを設計する際、どのカウンタを採用すべきかの指針となります。
ビット数をケチりたいならバイナリ、とにかく最高クロック周波数で動かしたいならRing、グリッチを絶対に許容できないセンサ系やFIFOポインタならグレイコード、というように適材適所で使い分けます。
-->

---

## 14. 順序回路におけるグリッチのメカニズム

なぜ多ビット変化は危険なのか？

```
バイナリ遷移 (01 -> 10) の物理波形:
bit 0: ──‾‾‾‾‾‾‾‾\_______  (立ち下がりがわずかに遅れた場合)
bit 1: _________/‾‾‾‾‾‾‾‾  (立ち上がりが早い)
過渡状態:   [01]   [11]   [10]  <── 一瞬だけ "11" が出現！
```

- この一瞬の `11` が後段の組合せ回路を通ると、予期せぬ**グリッチパルス**となって誤動作を引き起こす。
- **1ビットのみ変化するカウンタ (Johnson / Gray)** はこの過渡状態が原理的に存在しない。

<!-- Note:
グリッチ発生のメカニズムです。
物理的な配線やトランジスタには必ず遅延のバラツキがあります。
01から10に切り替わるとき、bit1が立ち上がるタイミングとbit0が立ち下がるタイミングが1ピコ秒でもズレると、一瞬だけ「11」という偽の状態が現れます。
これが後段のクロックやリセットに繋がっていると、致命的な誤動作を引き起こします。
-->

---

## 15. 本日の演習課題 (90分)

演習は**基本問題**と**発展問題**の2段階構成です。

### 基本問題（必須 / 目安45〜50分）
1. **`SelfCorrectingRingCounter`**:
   - `PopCount` を用いた1-hot不正状態の検出
   - 自動復帰付き循環シフトレジスタの実装
2. **`JohnsonCounter`**: 最上位ビット反転帰還による8状態巡回回路の実装

### 発展問題（推奨 / 目安30〜35分）
- **`GrayCodeCounter`**:
  - 内部バイナリカウンタとグレイコード変換回路の統合
  - 全遷移におけるハミング距離1動作の確認

### バッファ (5〜10分)
- コード整形、テスト実行、質疑応答

<!-- Note:
演習の案内です。
基本問題では、Ringカウンタの自己復帰論理をしっかり組んでください。テストベンチで意図的に不正な値を注入したときに、自動で 0001 に戻るかを確認します。
発展問題ではグレイコードカウンタに挑戦し、隣接コードが1ビットしか変わらないことを波形で確かめてください。
-->

---

## 16. 第4回 まとめ & 次回予告

### 本日の重要ポイント
1. シフトレジスタは `Cat` とビットスライスで加算器なしに高速に構成できる。
2. Ringカウンタは1-hot遷移により**デコーダ不要・最高速動作**を実現する。
3. Johnsonカウンタは反転帰還により $2N$ 状態を巡回し、2入力ANDでデコード可能。
4. 未使用状態へのトラップを防ぐため、**自己復帰論理**の実装が必須である。

### 次回予告: 第5回「構造化設計」
- 大規模回路を統制する階層化設計 (Hierarchical Design)
- サブモジュールのインスタンス化とポート配線 (`io <> io`)
- 算術ユニットと論理ユニットを統合したALUの階層設計

<!-- Note:
第4回の講義は以上となります。
特殊カウンタの持つ高速性と低ノイズ性、そして自己復帰設計の重要性を実感していただけたかと思います。
それでは演習に移ります。starter_kitのExercises.scalaのLab04を開いて作業を開始してください。
-->
