---
marp: true
theme: default
paginate: true
header: "第6回: 60進カウンタの構造化設計"
footer: "© 2026 Hideaki YANAGISAWA, Dept. of Computer Science and Electronic Engineering, NIT, Tokuyama College"
author: "Hideaki YANAGISAWA"
style: |
  section {
    font-family: 'Helvetica Neue', Arial, 'Hiragino Kaku Gothic ProN', sans-serif;
    font-size: 22px;
    padding: 32px 40px;
  }
  h1 { color: #1e3a8a; font-size: 34px; }
  h2 { color: #1e40af; border-bottom: 2px solid #3b82f6; font-size: 26px; }
  pre { font-size: 16px; margin: 8px 0; }
  .box { background: #eff6ff; border-left: 5px solid #3b82f6; padding: 8px 14px; margin: 8px 0; }
  .badge { background: #dbeafe; color: #1e40af; padding: 2px 6px; border-radius: 4px; font-weight: bold; font-size: 16px; }
---

# 第6回: 60進カウンタの構造化設計
### 〜10進×6進カスケードと同期イネーブル伝播アーキテクチャ〜

<br>

- **資料作成者**: Hideaki YANAGISAWA
- **講義時間**: 60分
- **対象**: デジタル論理回路基礎習得者
- **本日のアジェンダ**:
  1. 異進数カウンタの組み合わせによる時間計測回路
  2. **【最重要工学原則】非同期リップルクロックの完全排除**
  3. 単一グローバルクロックと**同期イネーブル伝播 (`cout` $\to$ `en`)**
  4. 10進カウンタ（1の位）と 6進カウンタ（10の位）の協調動作
  5. 桁上げ同期パルス (`rollover`) のタイミング検証
  6. 構造化60進カウンタ (`Counter60`) の完全実装
  7. 発展トピック: 時・分・秒を統合した24時間デジタル時計コア
  8. 本日の演習課題（基本: 60進カウンタ / 発展: デジタル時計コア）

<!-- Note:
第6回講義へようこそ。
本講義では、第3回で設計した「モジュロNカウンタ」と、第5回で学んだ「構造化設計」を融合させ、時計の秒・分を司る「60進カウンタ」を設計します。
60進カウンタは 10進カウンタ と 6進カウンタ の積として実現しますが、ここで初心者が必ず犯す致命的な過ちが「非同期リップルクロック」です。
本講義では、なぜ非同期クロックがダメなのか、そして真のプロはどうやって単一クロックで多段カウンタを同期連携させるのかを徹底的に解説します。
-->

---

## 1. 60進カウンタ (Modulo-60 Counter) の必要性

### 時計システム（Timekeeping System）の基本単位
- 1秒パルス（1Hz）を受けて、**秒 ($0 \sim 59$)** を計数する。
- 秒が59から0に戻る瞬間に桁上げパルスを発生させ、**分 ($0 \sim 59$)** を進める。
- 分が59から0に戻る瞬間に桁上げを発生させ、**時 ($0 \sim 23$)** を進める。

### 60進数をどう構成するか？
直接60進カウンタ（0〜59を数える6ビットカウンタ）を作ることも可能ですが、7セグメント表示器等に出力する場合、**「1の位（0〜9: 10進数）」と「10の位（0〜5: 6進数）」に最初から分離（BCD形式）して構造化**する方が圧倒的に扱いやすくなります。

$$60 = 10 \times 6$$

<!-- Note:
時計回路の設計動機です。
60進カウンタを0〜59の2進数として作ってしまうと、LEDディスプレイに表示する際に「59を10で割って商の5と余りの9を求める」という重い除算回路が後段に必要になってしまいます。
しかし、最初から「1の位の10進カウンタ」と「10の位の6進カウンタ」を階層的に組み合わせれば、割り算回路は一切不要になります。
-->

---

## 2. 【最重要工学原則】非同期リップルクロックの絶対禁止

昔の入門書などで見かける「下位カウンタの桁上げ出力を、上位カウンタのクロック入力に繋ぐ」回路。

```
 ─── BAD ARCHITECTURE (絶対にやってはならない設計) ───
 System Clock ──> [ 10進カウンタ ]
                         │ rollover (桁上げ)
                         ▼ (クロックとして供給！)
                  [ 6進カウンタの clock 入力 ] ───> 致命的障害！
```

<div class="box">
<b>なぜ非同期リップルクロックはダメなのか？</b><br>
1. <b>クロックスキューの累積</b>: 10進カウンタのフリップフロップ遅延が、6進カウンタのクロック遅延となり、段数が増えるほどタイミングがズレる。<br>
2. <b>グリッチによる誤カウント</b>: 桁上げ信号に生じたヒゲ（グリッチ）がクロックパルスと誤認され、余分にカウントアップする。<br>
3. <b>静的タイミング解析 (STA) の破綻</b>: 異なるクロックドメインが混在し、自動配置配線ツールでタイミング収束ができなくなる。
</div>

<!-- Note:
本講義で最も強く強調したいスライドです。
初心者が回路図を描くとき、10進カウンタのcoutを6進カウンタのクロック端子に繋いでしまいがちです。
これは「リップルクロック（非同期クロック）」と呼ばれ、現代の大規模集積回路（ASICやFPGA）では厳禁とされています。
クロックに遅延（スキュー）が生じ、桁上げ信号のわずかなグリッチで上位桁が2回進んでしまうといった怪現象が多発するためです。
Chiselでも、暗黙の単一クロックを使うことでこの悪習を構造的に排除しています。
-->

---

## 3. 正しい設計原則: 同期イネーブル伝播 (Synchronous Enable Propagation)

すべてのカウンタに**同一のシステムクロック（System Clock）**を供給し、下位の桁上げを**イネーブル信号 (`en`)** として上位へ伝播させる。

```
 ─── GOOD ARCHITECTURE (完全同期カスケード) ───
 System Clock ─────────────────────────────┬─────────────────────────────┐
                                           │                             │ (同一クロック)
                                           ▼                             ▼
 en ──────────> [ 10進カウンタ ] ───> [ 桁上げ論理 ] ───> en ───> [ 6進カウンタ ]
 (1秒パルス)        (0〜9)         rollover10 (1サイクル)           (0〜5)
```

- **単一クロックドメイン**: すべてのD-FFがナノ秒単位で完全に同時にスイッチングする。
- **グリッチフリー**: クロックの立ち上がりエッジでのみ状態が更新されるため、エッジ間のグリッチは完全に無視される。

<!-- Note:
これがプロのデジタル回路設計者の標準解です。
10進カウンタも6進カウンタも、全く同じメインクロック（例えば100MHzや50MHz）で駆動します。
そして、10進カウンタが「9」に達したとき、rollover信号を1クロックだけHighにします。
6進カウンタは、このrollover信号をクロックではなく「イネーブル（許可）信号」として受け取り、次のクロックエッジで1回だけカウントアップします。
-->

---

## 4. 同期カスケードのタイミング波形詳細

10進カウンタが $9$ から $0$ へ、6進カウンタが $0$ から $1$ へ遷移する瞬間。

```
CLK        : ──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──┐  ┌──
               └──┘  └──┘  └──┘  └──┘  └──┘  └──┘  └──┘  
en         : ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
10進 count : ──X  8   X       9       X   0    X   1    X
rollover10 : ─────────────────|‾‾‾‾‾‾‾‾‾|──────────────── (1サイクルのみHigh)
6進 en     : ─────────────────|‾‾‾‾‾‾‾‾‾|────────────────
6進 count  : ──X             0        X        1        X
```

- クロックエッジ $\uparrow$ の瞬間、`rollover10` が High であるため、10進カウンタは $0$ にリセットされ、**同時に** 6進カウンタが $0 \to 1$ へインクリメントされる！
- ズレ（遅延）は一切なく、完全な同時遷移が実現する。

<!-- Note:
タイミング波形で確認しましょう。
9のサイクルにおいて、rollover10が立ち上がります。
そして次のクロックの立ち上がりエッジにおいて、10進カウンタが0に戻る動作と、6進カウンタが0から1に進む動作が「完全に同一のエッジ」で同時に発生します。
非同期設計のような時間のズレ（過渡状態）が一切生じないため、表示のチラつきや誤動作が皆無になります。
-->

---

## 5. 60進カウンタの階層ブロック図

```
           ┌────────────────────────────────────────────────────────┐
           │                      Counter60                         │
           │                                                        │
           │    io.en ──────┬──────────────────────┐                │
           │                │                      │                │
           │                ▼                      ▼                │
           │        ┌──────────────┐       ┌──────────────┐         │
           │        │ u10 (10進)   │       │ u6 (6進)     │         │
 io.clear ─┼───────>│ clear        │       │ clear        │         │
           │        │              │       │              │         │
           │        │ rollover ────┼──────>│ en           │         │
           │        │ count (0〜9) │       │ count (0〜5) │         │
           │        └───────┬──────┘       └──────┬───────┘         │
           │                │                     │                 │
           │                ▼                     ▼                 │
           │           io.secUnits           io.secTens             │
           │           (1の位: 4bit)         (10の位: 3bit)         │
           │                                                        │
           │ io.cout <────── [ io.en && u10.rollover && u6.rollover ]
           └────────────────────────────────────────────────────────┘
```

<!-- Note:
Counter60モジュールの内部ブロック図です。
第3回で作った ModuloNCounter を2個使います。
1つは `ModuloNCounter(10, 4)`、もう1つは `ModuloNCounter(6, 3)` です。
u10のrolloverと外部enのANDを取ってu6のenに入力します。
全体のキャリーアウト io.cout は、59秒（u10が9かつu6が5）のときにアサートされ、次段の「分カウンタ」へと送られます。
-->

---

## 6. 60進カウンタ (`Counter60`) のChisel実装

```scala
class Counter60 extends Module {
  val io = IO(new Bundle {
    val en       = Input(Bool())
    val clear    = Input(Bool())
    val secUnits = Output(UInt(4.W)) // 1の位 (0〜9)
    val secTens  = Output(UInt(3.W)) // 10の位 (0〜5)
    val cout     = Output(Bool())    // 59でアサート
  })

  // 1. サブモジュールのインスタンス化 (第3回のModuloNCounterを再利用)
  val u10 = Module(new ModuloNCounter(10, 4))
  val u6  = Module(new ModuloNCounter(6, 3))

  // 2. 10進カウンタの配線
  u10.io.clear := io.clear; u10.io.load := false.B; u10.io.loadData := 0.U
  u10.io.en    := io.en

  // 3. 6進カウンタへの同期イネーブル配線 (cout -> en)
  u6.io.clear  := io.clear; u6.io.load := false.B; u6.io.loadData := 0.U
  u6.io.en     := io.en && u10.io.rollover

  // 4. 出力ポートのバインド
  io.secUnits  := u10.io.count
  io.secTens   := u6.io.count
  io.cout      := io.en && u10.io.rollover && u6.io.rollover
}
```

<!-- Note:
Counter60の実装コードです。
非常にすっきりと書けています。
第3回で作った ModuloNCounter をそのまま2つ並べ、u6.io.en に `io.en && u10.io.rollover` を接続しています。
io.en をAND条件に入れているのは、全体のカウントが一時停止（en=0）しているときは、たとえ10進が9であっても6進が進まないようにするためです。
-->

---

## 7. 発展課題: 24時間デジタル時計コア (`DigitalClockCore`)

60進カウンタをさらに多段にカスケード接続し、時・分・秒を刻む時計システムを構築する。

```
 1Hzパルス ──> [ 秒カウンタ ] ───> cout (分イネーブル) ──> [ 分カウンタ ] ───> cout (時イネーブル) ──> [ 時カウンタ ]
                (60進: 0〜59)                              (60進: 0〜59)                             (24進: 0〜23)
                      │                                          │                                         │
                      ▼                                          ▼                                         ▼
                   io.sec (6bit)                              io.min (6bit)                             io.hour (5bit)
```

### システム構成要素
1. **秒カウンタ (`ModuloNCounter(60, 6)`)**: $0 \sim 59$ をカウント。
2. **分カウンタ (`ModuloNCounter(60, 6)`)**: 秒の `rollover` でインクリメント。
3. **時カウンタ (`ModuloNCounter(24, 5)`)**: 秒と分の両方の `rollover` でインクリメント。

<!-- Note:
発展課題で設計する24時間デジタル時計コアです。
秒・分・時という、人間が日常的に使っている時計の完全な論理回路を作ります。
秒の桁上げが分のイネーブルになり、秒と分の両方の桁上げが揃った瞬間（59分59秒）に時のイネーブルが立ち上がります。
これらもすべて単一のクロックで同期動作します。
-->

---

## 8. デジタル時計コアの同期桁上げ伝播式

### 各段のイネーブル条件式
- **秒カウンタのイネーブル**:
  $$en_{\text{sec}} = io.enSec$$
- **分カウンタのイネーブル**:
  $$en_{\text{min}} = io.enSec \land secCounter.rollover$$
- **時カウンタのイネーブル**:
  $$en_{\text{hour}} = io.enSec \land secCounter.rollover \land minCounter.rollover$$

<div class="box">
<b>桁上げ連鎖の論理積</b><br>
時カウンタを進める条件は「毎秒パルスがあり、秒が59であり、かつ分が59であること」です。下位のイネーブル条件をすべてANDで束ねることが、同期カスケードの基本原則です。
</div>

<!-- Note:
各カウンタを動かす条件式です。
時カウンタのイネーブルに注目してください。
単に minCounter.rollover だけを見るのではなく、必ず一番元の enSec と secCounter.rollover もAND条件に含めます。
59分である時間は丸々1分間続きますが、時が進むべき瞬間はその最後の1秒（59分59秒）だけだからです。
-->

---

## 9. 24時間デジタル時計コアの実装コード

```scala
class DigitalClockCore extends Module {
  val io = IO(new Bundle {
    val enSec = Input(Bool())     // 1秒周期ストローブパルス
    val clear = Input(Bool())
    val sec   = Output(UInt(6.W)) // 0〜59
    val min   = Output(UInt(6.W)) // 0〜59
    val hour  = Output(UInt(5.W)) // 0〜23
  })

  val secCnt = Module(new ModuloNCounter(60, 6))
  val minCnt = Module(new ModuloNCounter(60, 6))
  val hrsCnt = Module(new ModuloNCounter(24, 5))

  // 秒カウンタ配線
  secCnt.io.clear := io.clear; secCnt.io.load := false.B; secCnt.io.loadData := 0.U
  secCnt.io.en    := io.enSec

  // 分カウンタ配線 (59秒時に同期進行)
  minCnt.io.clear := io.clear; minCnt.io.load := false.B; minCnt.io.loadData := 0.U
  minCnt.io.en    := io.enSec && secCnt.io.rollover

  // 時カウンタ配線 (59分59秒時に同期進行)
  hrsCnt.io.clear := io.clear; hrsCnt.io.load := false.B; hrsCnt.io.loadData := 0.U
  hrsCnt.io.en    := io.enSec && secCnt.io.rollover && minCnt.io.rollover

  io.sec := secCnt.io.count; io.min := minCnt.io.count; io.hour := hrsCnt.io.count
}
```

<!-- Note:
デジタル時計コアの完成コードです。
ModuloNCounter を3つ階層インスタンス化し、見事に同期イネーブルで結線されています。
23時59分59秒の次の秒パルスで、秒は0、分は0、時は23から0へ戻り、見事に 00:00:00 へ同期リセットされます。
-->

---

## 10. クロック分周器 (Clock Divider) の工学的注意

実機基板（FPGA等）の水晶発振器は $50\text{MHz}$ や $100\text{MHz}$ で発振しています。ここから $1\text{Hz}$（1秒）の信号を作る方法：

<div style="display: flex; justify-content: space-between;">
<div style="width: 48%;">

### 危険な方法 (クロック分周)
- カウンタでクロック波形を作り、それを別モジュールの `clock` に繋ぐ。
- **NG**: クロックツリーに乗らず、FPGA内部の配線遅延でジッタとスキューが発生。

</div>
<div style="width: 48%;">

### 正しい方法 (ストローブパルス生成)
- 50MHzカウンタで、**1秒に1回だけ1クロック幅のHighパルス (`enSec`)** を出力。
- **OK**: システムクロックは50MHzのまま、`en` 端子で動作頻度を間引く。

</div>
</div>

<!-- Note:
実務で非常に重要なクロックイネーブルの概念です。
50MHzのクロックから1秒を作りたいとき、新しい1Hzの低速クロック線を作ってはいけません。
システム全体のクロックは50MHzのまま維持し、5000万回に1回だけ「1クロック分だけHighになるイネーブルパルス（ストローブ信号）」を作って en 端子に配線します。
これによってシステム全体が完全に単一クロックで調和して動作します。
-->

---

## 11. ファンアウト (Fan-out) と伝搬遅延の考察

時計コアのように桁上げ信号が多段に連鎖する場合：

$$io.enSec \land secCnt.rollover \land minCnt.rollover$$

- **ANDゲートの直列接続**: 段数が増えるほど組合せゲート遅延が加算される。
- **ファンアウト負荷**: `secCnt.rollover` は分カウンタだけでなく時カウンタのANDゲートにも供給されるため、配線容量が増加する。
- 数段程度（時分秒）であれば問題ないが、数十段に及ぶカウンタでは先回りキャリー（Carry Lookahead）と同様の並列化手法が必要になる。

<!-- Note:
回路の物理的特性についての深掘りです。
下位の桁上げ信号をANDゲートで束ねていくと、段数が増えるにつれてANDゲートの遅延が積み重なります。
時計のように3段程度であれば全く問題ありませんが、32ビットや64ビットのカウンタを組む場合は、遅延がクロック周期を超えないように注意する必要があります。
-->

---

## 12. カウンタ回路のコーナーケース検証シナリオ

テストベンチで必ず確認すべきクリティカルな境界遷移：

1. **秒の境界**: `09` $\to$ `10` 秒への遷移（1の位クリア、10の位インクリメント）
2. **分の境界**: `59` $\to$ `00` 秒への遷移（秒クリア、分インクリメント）
3. **時の境界**: `00:59:59` $\to$ `01:00:00` への同時3段遷移
4. **一日の境界**: `23:59:59` $\to$ `00:00:00` へのフルラップアラウンド
5. **一時停止**: カウント進行中に `enSec` を `false.B` にした際、現在時刻が完全に保持されるか

<!-- Note:
テスト検証のシナリオです。
デジタル回路のデバッグでは、最も多くのビットが一斉に切り替わる「境界点」を徹底的に攻めます。
23:59:59 から 00:00:00 への遷移は、3つのカウンタすべてが同時に上限に達してゼロに戻る瞬間です。
この瞬間の波形が1クロックのズレもなく綺麗に切り替わっているかを確認することが、品質保証の決め手です。
-->

---

## 13. モジュール再利用性（Reusability）の価値

本日の設計で体感できる構造化設計の威力：

- 第3回で作成した `ModuloNCounter` を**1行も変更することなく**、
  - 10進カウンタ (`n = 10`)
  - 6進カウンタ (`n = 6`)
  - 60進カウンタ (`n = 60`)
  - 24進カウンタ (`n = 24`)
  として自由自在に再利用している。
- これがChiselにおける**パラメータ化（Parametric IP設計）**の真髄。

<!-- Note:
再利用性についての講評です。
第3回で ModuloNCounter を作ったとき、「n: Int」という引数をつけておきました。
そのおかげで、今日60進カウンタやデジタル時計を作るときに、新しいカウンタをゼロから書く必要は一切ありませんでした。
パラメータを変えてインスタンス化するだけで、どんな進数のカウンタも即座に手に入ります。
-->

---

## 14. 故障診断とタイミング違反の防止まとめ

<div class="box">
<b>デジタルシステム設計のチェックリスト</b><br>
□ 回路内のすべてのモジュールが同一の <code>clock</code> を共有しているか？<br>
□ 下位モジュールの出力を直接 <code>clock</code> ピンに接続していないか？<br>
□ 桁上げはすべて <code>en</code> 入力を介した同期イネーブルとして配線されているか？<br>
□ リセット解除時、すべてのカウンタが同時に初期化されるか？
</div>

<!-- Note:
まとめのチェックリストです。
企業の実務現場でも、ASICやFPGAの設計レビューで必ず確認される項目です。
非同期リップルクロックを根絶し、同期イネーブルで統一されていること。
これさえ守られていれば、実機基板に書き込んだときに「動いたり動かなかったりする」という厄介なバグに悩まされることはありません。
-->

---

## 15. 本日の演習課題 (90分)

演習は**基本問題**と**発展問題**の2段階構成です。

### 基本問題（必須 / 目安45〜50分）
- **`Counter60`**:
  - `ModuloNCounter(10, 4)` と `ModuloNCounter(6, 3)` のインスタンス化
  - 同期イネーブル (`cout` $\to$ `en`) によるカスケード接続
  - 1の位 (`secUnits`) と 10の位 (`secTens`)、および `cout` の出力

### 発展問題（推奨 / 目安30〜35分）
- **`DigitalClockCore`**:
  - 秒(60進)、分(60進)、時(24進)の3段同期カスケード結合
  - 23:59:59 $\to$ 00:00:00 の完全自動巡回動作の検証

### バッファ (5〜10分)
- コード整形、テスト実行、波形確認

<!-- Note:
演習の案内です。
まずは基本問題で10進と6進を組み合わせたCounter60を完成させましょう。
その後、発展課題で時・分・秒のデジタル時計コアに進んでください。
時計が正確に時を刻むシミュレーション波形を見たときの感動は格別です。
-->

---

## 16. 第6回 まとめ & 次回予告

### 本日の重要ポイント
1. 60進カウンタは 10進×6進 の構造化によりBCD形式で効率的に設計できる。
2. **非同期リップルクロックは厳禁**。単一クロックと**同期イネーブル伝播**を用いる。
3. 下位の桁上げパルス (`rollover`) を上位の `en` 入力に接続する。
4. カスケード段数が増えても、すべて同一のクロックエッジで完全同期遷移する。

### 次回予告: 第7回「有限状態機械 (FSM)」
- 順序回路の最高峰: 複雑な制御論理を司るステートマシン
- **Moore型 vs Mealy型**: なぜMoore型は出力がグリッチフリーなのか
- Chiselにおける列挙型 `ChiselEnum` を用いた型安全な状態遷移設計

<!-- Note:
第6回の講義は以上です。
構造化と同期イネーブルの威力を実感できたでしょうか。
次回はいよいよ、ハードウェア制御の最高峰である「有限状態機械（FSM）」を学びます。
それでは演習に移ります。starter_kitのExercises.scalaのLab06を開いてください。
-->
