# 全8回 演習ヒント集 (Engineering Hints)

- **作成担当**: `04_課題作成担当`
- **授業名**: 「Computer System Laboratory」
- **構成**: 全8回の「基本問題」および「発展問題」に対する技術的ヒントと注意点

---

## 第1回: 組合せ回路の基礎

### 基本問題ヒント (`FullAdder`, `RippleCarryAdder4`)
1. **ブール代数の直訳**: 全加算器の和 $sum$ は3入力の排他的論理和 (`a ^ b ^ cin`)、桁上げ $cout$ は多数決論理 (`(a & b) | (cin & (a ^ b))`) です。
2. **多ビット加算のビット幅**: 4ビット同士を加算すると結果は最大5ビットになります。Chiselの拡張加算 `a +& b` を使うと、桁あふれを失わずにキャリーを含む5ビットの `UInt` が得られます。
3. **ビットスライスと結合**: リップルキャリーを構成する場合、`a(0)`, `b(0)` から順にインスタンス化するか、ループで `sum` ベクトルを `Cat` で結合します。

### 発展問題ヒント (`AddSub4`)
1. **2の補数の減算原理**: $A - B = A + \overline{B} + 1$ です。したがって、制御信号 `sub` が `true.B` のときは、各ビット反転 `b ^ Fill(4, sub)`（またはビットごとの反転）を入力し、最下位キャリーインに `sub` を供給します。
2. **オーバーフロー検出**: 符号付き加減算におけるオーバーフローは、「同じ符号の数を足したのに結果の符号が反転した場合」に発生します。最上位ビット (MSB: bit[3]) に着目し、加算時の被加数・加数・結果の符号関係から判定式を論理ゲートで組み立てます。

---

## 第2回: Muxとデコーダの回路

### 基本問題ヒント (`Mux4to1`, `SevenSegDecoder`)
1. **透過ラッチの撲滅 (`WireDefault`)**: `SevenSegDecoder` では出力 `seg` のデフォルト値を必ず宣言時に `WireDefault(0.U(7.W))` で指定してください。これにより `switch / is` で記述されなかった入力値（10〜15等）に対しても明示的に値が定まり、意図しないラッチ生成を完全に防止できます。
2. **2-to-1 MUXの木構造**: `Mux4to1` は `Mux(sel(1), Mux(sel(0), in3, in2), Mux(sel(0), in1, in0))` のように2-to-1 MUXを階層的に組み合わせることで簡潔に記述できます。

### 発展問題ヒント (`PriorityEncoder4`)
1. **優先順位の表現**: `when / elsewhen / otherwise` は上から順に優先評価されるハードウェア（優先度マルチプレクサ連鎖）を生成します。最高位ビット `in(3)` から順に `when(in(3)) { pos := 3.U }` と判定していくことで、自然に優先度エンコーダが合成されます。
2. **有効信号 (`valid`)**: 入力の全ビット論理和 (`in.orR`) を用いると、1行で簡潔かつグリッチの少ないフラグ生成が可能です。

---

## 第3回: 順序回路と基本カウンタ

### 基本問題ヒント (`ModuloNCounter`)
1. **優先度構造**: クロック同期回路の制御論理では、`clear` が最優先、次が `load`、その次が `en` の順序で `when / elsewhen` を構成します。
2. **端数検出とラップアラウンド**: $N$ 進カウンタは $0$ から $N-1$ までの $N$ 個の値を数えます。したがってリセット条件は `count === (n - 1).U` です。不等号 `<` や `>` を使うと余分な比較器ロジックが合成されるため、一致比較 `===` を用いるのが定石です。

### 発展問題ヒント (`PwmGenerator`)
1. **比較器の出力タイミング**: カウンタが周期値 `period` に達したら自動で0にクリアし、同時に現在のカウント値と `duty` を大小比較 (`cnt < duty`) します。
2. **デューティ比100%と0%**: `duty === 0.U` のときは常にLow、`duty > period` のときは常にHighになるようなエッジケースの挙動を意識して比較論理を設計してください。

---

## 第4回: RingカウンタとJohnsonカウンタ

### 基本問題ヒント (`SelfCorrectingRingCounter`, `JohnsonCounter`)
1. **1-hot判定と復帰**: 4ビットRingカウンタの正常状態は `0001`, `0010`, `0100`, `1000` の4つのみです。立っているビット数を数える `PopCount(out) === 1.U` を判定論理に用いることで、万が一外乱やノイズで多重ビットが立ったり全ゼロになった場合でも、次クロックで `0001.U` に復帰させることができます。
2. **Johnsonカウンタの帰還**: 最上位ビット `out(width-1)` を取り出し、それを反転した信号 `~out(width-1)` を下位ビット `out(width-2, 0)` と結合（`Cat(out(width-2, 0), ~out(width-1))`）して次状態レジスタへ入力します。

### 発展問題ヒント (`GrayCodeCounter`)
1. **変換式の簡潔性**: バイナリコード $B$ からグレイコード $G$ への変換は $G = B \oplus (B \gg 1)$ で表されます。Chiselでは `val gray = bin ^ (bin >> 1)` と直感的に記述できます。
2. **非同期ハザード耐性**: グレイコードは隣接する遷移で1ビットしか変化しないため、非同期回路との境界やFIFOのポインタ受け渡しでグリッチを生じない特長があります。内部カウンタ `bin` は標準同期カウンタとして進め、出力段で変換します。

---

## 第5回: 構造化設計

### 基本問題ヒント (`StructuredALU`)
1. **サブモジュールのインスタンス化**:
   ```scala
   val arith = Module(new ArithmeticUnit)
   val logic = Module(new LogicUnit)
   ```
2. **ポート接続の作法**: 上位モジュールの入力を各サブモジュールの入力へ並列に分配し、各サブモジュールの出力をマルチプレクサ（セレクタ）で受けて上位出力 `io.out` へ接続します。

### 発展問題ヒント (`PipelinedALUWithFlags`)
1. **レジスタ挿入**: 組合せALUの出力に `val outReg = RegNext(aluOut)` を配置して1クロック遅延させることで、クリティカルパスを分断し動作周波数を向上させます。
2. **制御信号の整合性**: データパスを1クロック遅延（パイプライン化）させた場合、対応する `valid` 信号やステータスフラグも同じタイミングで1クロック遅延させる必要があります (`RegNext(validIn)`)。

---

## 第6回: 60進カウンタの構造化設計

### 基本問題ヒント (`Counter60`)
1. **絶対禁止事項（非同期クロック）**: 10進カウンタの桁上げ出力を、6進カウンタの `clock` 入力に接続してはいけません（クロックスキューやタイミング収束不能の原因）。
2. **同期イネーブル伝播**: 両方のカウンタに同一のシステムクロックを与え、10進カウンタが 9 に達した瞬間（`rollover10 === true.B`）かつ全体の `en === true.B` のときにのみ、6進カウンタの `en` 入力を 1 クロックだけ有効化します。

### 発展問題ヒント (`DigitalClockCore`)
1. **階層カスケードの連鎖**:
   - 秒の桁上げ: 59秒時に発生 $\to$ 分カウンタのイネーブルへ
   - 分の桁上げ: 59分59秒時に発生 $\to$ 時カウンタのイネーブルへ
2. **24進カウンタの境界条件**: 時カウンタは24進 (`ModuloNCounter(24, 5)`) です。23時59分59秒の次のクロックで全カウンタが 00:00:00 に同期してクリアされることを確認してください。

---

## 第7回: 有限状態機械 (FSM)

### 基本問題ヒント (`SequenceDetector101`)
1. **ChiselEnumの定義**:
   ```scala
   object State extends ChiselEnum {
     val sIDLE, s1, s10, s101 = Value
   }
   ```
2. **状態遷移の網羅**: 各状態において入力 `in` が `0.B` の場合と `1.B` の場合の両方の行き先を明示的に記述します。特に `s101` の状態で `1` が来た場合、直前の "1" を次の "101" の先頭とみなして `s1` へ遷移させることで、重複シーケンスを漏れなく検出できます。
3. **Moore型出力**: 出力 `detected` は現在の状態レジスタ `state === State.s101` のみで判定し、入力 `in` を直接混ぜないことでグリッチを完全に防ぎます。

### 発展問題ヒント (`TrafficLightController`)
1. **タイマカウンタとの連携**: FSM内にサイクル数を数えるタイマレジスタ `val timer = RegInit(0.U(8.W))` を設け、「一定サイクル経過したら次状態へ自動遷移」するパターンを作ります。
2. **リクエストのラッチ**: 歩行者ボタン `pedestrianButton` はいつ押されるか分からないため、押されたら `val pedRequest = RegInit(false.B)` を `true.B` に保持し、歩行者青信号が終わった段階で `false.B` にクリアします。

---

## 第8回: メモリ回路と非同期読み出し

### 基本問題ヒント (`RegisterFile8x8`)
1. **非同期読み出しの記述**:
   ```scala
   val mem = Mem(8, UInt(8.W))
   when(io.wen) { mem(io.waddr) := io.wdata }
   io.rdata1 := mem(io.raddr1) // アドレス変化と同一サイクルに出力
   ```
2. **マルチリードポート**: `mem` インスタンスに対して異なるアドレス `io.raddr1` と `io.raddr2` で非同期インデックス参照を行うだけで、独立した2つの読み出しポートが合成されます。

### 発展問題ヒント (`BypassedRegisterFile`)
1. **フォワーディング比較条件**:
   - `val bypass1 = io.wen && (io.waddr === io.raddr1)`
   - `io.rdata1 := Mux(bypass1, io.wdata, mem(io.raddr1))`
2. **ハードウェアの意図**: メモリへの書き込みデータが確定するクロックエッジを待たずに、同じクロックサイクル内で書き込みデータを読み出しポートへバイパスさせることで、パイプラインハザードやストールを削減するプロセッサ設計の基本技術です。
